export interface EditorialContent {
  title: string;
  description: string;
  path: string;
  intent: string;
  schema: string[];
  internalLinks: string[];
  markdown: string;
  html: string;
}

export const educationContent: Record<string, EditorialContent> = {
  "/fine-tune-llm-on-mac": {
    "title": "How to fine-tune an LLM on a Mac",
    "description": "A practical, evidence-first workflow for fine-tuning an LLM on Apple Silicon with a defined target, LoRA or QLoRA, evaluation, packaging, and reporting.",
    "path": "/fine-tune-llm-on-mac",
    "intent": "category and workflow guide for developers fine-tuning language models on a Mac",
    "schema": [
      "HowTo",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/mlx-lora-fine-tuning",
      "/evaluate-local-llm",
      "/agent-trajectory-dataset",
      "/build-small-language-model-specialist",
      "/artifacts",
      "/recipes"
    ],
    "markdown": "Fine-tuning an LLM on a Mac is practical when the target is narrow, the model fits Apple Silicon memory, and the evaluation is designed before training begins. The useful goal is rarely to build a smaller general chatbot. It is to create a specialist that improves on a defined task, routes away from tasks it should not answer, and ships with evidence about both gains and regressions.\n\n## Start with a target, not a trainer\n\nWrite down the task, input and output contract, baseline model, acceptance slices, and failure conditions. A target such as file operations, SQL hygiene, or intent routing can be evaluated. “Make the model smarter” cannot. Freeze a holdout before generating or cleaning training data.\n\n## Choose a method that fits the Mac\n\nLoRA updates a small set of adapter weights while leaving the base model frozen. QLoRA applies low-rank adaptation to a quantized base and can reduce memory pressure further. Full-weight fine-tuning is possible for smaller models but is not the default starting point for a one-machine workflow. Apple’s MLX LM supports text generation, quantization, LoRA, QLoRA, and full fine-tuning on Apple Silicon.\n\n## Build the factory loop\n\n1. **Target:** define the behavior and routing boundary.\n2. **Data:** create train, validation, and frozen test splits with provenance.\n3. **Post-train:** run a bounded recipe and preserve the configuration.\n4. **Evaluate:** compare the unchanged baseline and candidate on the same gates.\n5. **Package:** export the adapter or fused model with runtime metadata.\n6. **Report:** publish gains, regressions, missing measurements, and the ship, retry, or reject decision.\n\nPostTrainLLM implements this loop as a Mac-local specialist factory. Its public evidence includes specialist packages and report cards, including a file-operations specialist that improves its hard gate from 58% to 100% while regressing out-of-domain breadth from 59.6% to 42.3%. That tradeoff is why it is routed rather than presented as a general planner.\n\n## What success looks like\n\nSuccess is a reproducible candidate that beats the frozen baseline on the target, stays within explicit regression limits, fits the intended runtime, and has enough evidence to make a decision. A falling training loss is only an intermediate signal.\n\n**CTA:** Inspect the public specialist artifacts, then follow the MLX LoRA and evaluation guides.",
    "html": "<p>Fine-tuning an LLM on a Mac is practical when the target is narrow, the model fits Apple Silicon memory, and the evaluation is designed before training begins. The useful goal is rarely to build a smaller general chatbot. It is to create a specialist that improves on a defined task, routes away from tasks it should not answer, and ships with evidence about both gains and regressions.</p>\n<h2>Start with a target, not a trainer</h2>\n<p>Write down the task, input and output contract, baseline model, acceptance slices, and failure conditions. A target such as file operations, SQL hygiene, or intent routing can be evaluated. “Make the model smarter” cannot. Freeze a holdout before generating or cleaning training data.</p>\n<h2>Choose a method that fits the Mac</h2>\n<p>LoRA updates a small set of adapter weights while leaving the base model frozen. QLoRA applies low-rank adaptation to a quantized base and can reduce memory pressure further. Full-weight fine-tuning is possible for smaller models but is not the default starting point for a one-machine workflow. Apple’s MLX LM supports text generation, quantization, LoRA, QLoRA, and full fine-tuning on Apple Silicon.</p>\n<h2>Build the factory loop</h2>\n<ol>\n<li><strong>Target:</strong> define the behavior and routing boundary.</li>\n<li><strong>Data:</strong> create train, validation, and frozen test splits with provenance.</li>\n<li><strong>Post-train:</strong> run a bounded recipe and preserve the configuration.</li>\n<li><strong>Evaluate:</strong> compare the unchanged baseline and candidate on the same gates.</li>\n<li><strong>Package:</strong> export the adapter or fused model with runtime metadata.</li>\n<li><strong>Report:</strong> publish gains, regressions, missing measurements, and the ship, retry, or reject decision.</li>\n</ol>\n<p>PostTrainLLM implements this loop as a Mac-local specialist factory. Its public evidence includes specialist packages and report cards, including a file-operations specialist that improves its hard gate from 58% to 100% while regressing out-of-domain breadth from 59.6% to 42.3%. That tradeoff is why it is routed rather than presented as a general planner.</p>\n<h2>What success looks like</h2>\n<p>Success is a reproducible candidate that beats the frozen baseline on the target, stays within explicit regression limits, fits the intended runtime, and has enough evidence to make a decision. A falling training loss is only an intermediate signal.</p>\n<p><strong>CTA:</strong> Inspect the public specialist artifacts, then follow the MLX LoRA and evaluation guides.</p>\n"
  },
  "/mlx-lora-fine-tuning": {
    "title": "MLX LoRA fine-tuning on Apple Silicon",
    "description": "Learn the MLX LoRA and QLoRA workflow on Apple Silicon, from JSONL data and bounded training to evaluation, fusion, and evidence.",
    "path": "/mlx-lora-fine-tuning",
    "intent": "technical how-to for MLX LoRA fine-tuning",
    "schema": [
      "HowTo",
      "TechArticle",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/fine-tune-llm-on-mac",
      "/evaluate-local-llm",
      "/artifacts",
      "/recipes",
      "/docs/factory/run-schema"
    ],
    "markdown": "MLX LM provides a native Apple Silicon path for adapting open language models with LoRA or QLoRA. The command is the easy part. The quality of the result depends on the dataset contract, the frozen baseline, and the evaluation you run after the adapter trains.\n\n## Prepare the environment and data\n\nInstall the training extras for MLX LM and choose a compatible model. The official loader accepts a local data directory with `train.jsonl`, optional `valid.jsonl`, and `test.jsonl`, or supported Hugging Face datasets. Chat, tool, completion, and text formats are supported. Keep each JSONL example on one line and validate the chat template for the chosen base model.\n\n## Run a bounded adapter experiment\n\nUse `mlx_lm.lora` with the base model, data path, iteration budget, and an explicit adapter output path. A quantized base selects QLoRA; a non-quantized base uses LoRA by default. Record the exact base revision, MLX LM version, configuration, seed, data manifest, and machine. If you use prompt masking, verify that the loss is applied to the intended completion tokens.\n\n## Evaluate before fusing\n\nTest the unchanged base and the adapter on the same frozen examples. Perplexity can detect some training problems but does not replace task metrics. For a tool specialist, measure exact tool selection and arguments. For a formatter, measure protected spans and unnecessary edits. Add out-of-domain slices so a narrow gain cannot hide general regression.\n\n## Generate and package\n\nUse the adapter with `mlx_lm.generate` for qualitative inspection only after the quantitative gate. Fuse only when the evaluation decision supports it. The official MLX LM workflow can fuse adapter weights and export GGUF, but the resulting artifact still needs a model card, base identity, license checks, runtime requirements, and a record of missing measurements.\n\n## Common failure modes\n\n- Training and evaluating on overlapping examples.\n- Choosing a ship threshold after seeing the candidate.\n- Treating lower loss as task success.\n- Publishing only the improved slice.\n- Fusing before preserving the adapter, recipe, and evidence.\n- Assuming a narrow specialist should replace the general model.\n\nPostTrainLLM wraps these concerns in a factory-run, evaluation, package, and report-card workflow so the result is a decision artifact rather than an orphan adapter folder.\n\n**CTA:** Read the local evaluation guide or inspect a public specialist report card.",
    "html": "<p>MLX LM provides a native Apple Silicon path for adapting open language models with LoRA or QLoRA. The command is the easy part. The quality of the result depends on the dataset contract, the frozen baseline, and the evaluation you run after the adapter trains.</p>\n<h2>Prepare the environment and data</h2>\n<p>Install the training extras for MLX LM and choose a compatible model. The official loader accepts a local data directory with <code>train.jsonl</code>, optional <code>valid.jsonl</code>, and <code>test.jsonl</code>, or supported Hugging Face datasets. Chat, tool, completion, and text formats are supported. Keep each JSONL example on one line and validate the chat template for the chosen base model.</p>\n<h2>Run a bounded adapter experiment</h2>\n<p>Use <code>mlx_lm.lora</code> with the base model, data path, iteration budget, and an explicit adapter output path. A quantized base selects QLoRA; a non-quantized base uses LoRA by default. Record the exact base revision, MLX LM version, configuration, seed, data manifest, and machine. If you use prompt masking, verify that the loss is applied to the intended completion tokens.</p>\n<h2>Evaluate before fusing</h2>\n<p>Test the unchanged base and the adapter on the same frozen examples. Perplexity can detect some training problems but does not replace task metrics. For a tool specialist, measure exact tool selection and arguments. For a formatter, measure protected spans and unnecessary edits. Add out-of-domain slices so a narrow gain cannot hide general regression.</p>\n<h2>Generate and package</h2>\n<p>Use the adapter with <code>mlx_lm.generate</code> for qualitative inspection only after the quantitative gate. Fuse only when the evaluation decision supports it. The official MLX LM workflow can fuse adapter weights and export GGUF, but the resulting artifact still needs a model card, base identity, license checks, runtime requirements, and a record of missing measurements.</p>\n<h2>Common failure modes</h2>\n<ul>\n<li>Training and evaluating on overlapping examples.</li>\n<li>Choosing a ship threshold after seeing the candidate.</li>\n<li>Treating lower loss as task success.</li>\n<li>Publishing only the improved slice.</li>\n<li>Fusing before preserving the adapter, recipe, and evidence.</li>\n<li>Assuming a narrow specialist should replace the general model.</li>\n</ul>\n<p>PostTrainLLM wraps these concerns in a factory-run, evaluation, package, and report-card workflow so the result is a decision artifact rather than an orphan adapter folder.</p>\n<p><strong>CTA:</strong> Read the local evaluation guide or inspect a public specialist report card.</p>\n"
  },
  "/evaluate-local-llm": {
    "title": "How to evaluate a local LLM",
    "description": "An evidence-first workflow for evaluating a local LLM against a frozen baseline, task gates, regression slices, and an explicit decision.",
    "path": "/evaluate-local-llm",
    "intent": "how-to for developers evaluating local and fine-tuned language models",
    "schema": [
      "HowTo",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/fine-tune-llm-on-mac",
      "/mlx-lora-fine-tuning",
      "/eval-leaderboard",
      "/leaderboard",
      "/artifacts",
      "/recipes"
    ],
    "markdown": "A local LLM evaluation should answer a product decision, not merely produce a leaderboard number. Freeze the target and baseline first, run the baseline and candidate through the same protocol, inspect task-specific slices and regressions, then record a ship, route, retry, or reject decision.\n\n## Define the contract\n\nSpecify the inputs, expected outputs, allowed variation, protected content, and failure classes. Choose metrics that match the task: exact execution for SQL, tool and argument correctness for agents, unnecessary edit rate for autocorrection, or grounded answer criteria for retrieval. Keep a qualitative review lane, but do not let it overwrite deterministic gates.\n\n## Freeze the evaluation set\n\nSeparate training, validation, and test data. Record provenance and deduplicate across splits. A holdout that changes for every candidate cannot support a fair comparison. Mark synthetic fixtures as synthetic and avoid describing a small suite as production proof.\n\n## Run baseline and candidate symmetrically\n\nUse the same prompts, decoding settings, runtime, and evaluator. Preserve raw predictions and measurement state. Record timeouts and missing values separately from zero. When the candidate is a routed specialist, evaluate both the target slice and the routing boundary.\n\n## Measure regressions explicitly\n\nA specialist can improve the hard gate and still become worse outside it. Report deltas for every protected slice. Define reject conditions before the run, such as meaning changes, leakage, breadth loss beyond a threshold, or latency and memory outside the target machine budget.\n\n## Make the decision reproducible\n\nPackage the base and candidate identities, dataset revision, evaluator revision, configuration, per-slice results, missing measurements, and final decision. Keep “ship” narrower than “the candidate improved.” A routed ship can be correct when the target gain is real and the routing constraint is enforced.\n\nPostTrainLLM includes fixture and no-GPU gates, live eval commands, canonical factory-run records, specialist packages, and public report cards. Several public results intentionally disclose missing historical latency, RAM, throughput, or raw traces rather than replacing those gaps with estimates.\n\n**CTA:** Inspect the eval leaderboard and report cards, then use the factory recipes to reproduce the workflow.",
    "html": "<p>A local LLM evaluation should answer a product decision, not merely produce a leaderboard number. Freeze the target and baseline first, run the baseline and candidate through the same protocol, inspect task-specific slices and regressions, then record a ship, route, retry, or reject decision.</p>\n<h2>Define the contract</h2>\n<p>Specify the inputs, expected outputs, allowed variation, protected content, and failure classes. Choose metrics that match the task: exact execution for SQL, tool and argument correctness for agents, unnecessary edit rate for autocorrection, or grounded answer criteria for retrieval. Keep a qualitative review lane, but do not let it overwrite deterministic gates.</p>\n<h2>Freeze the evaluation set</h2>\n<p>Separate training, validation, and test data. Record provenance and deduplicate across splits. A holdout that changes for every candidate cannot support a fair comparison. Mark synthetic fixtures as synthetic and avoid describing a small suite as production proof.</p>\n<h2>Run baseline and candidate symmetrically</h2>\n<p>Use the same prompts, decoding settings, runtime, and evaluator. Preserve raw predictions and measurement state. Record timeouts and missing values separately from zero. When the candidate is a routed specialist, evaluate both the target slice and the routing boundary.</p>\n<h2>Measure regressions explicitly</h2>\n<p>A specialist can improve the hard gate and still become worse outside it. Report deltas for every protected slice. Define reject conditions before the run, such as meaning changes, leakage, breadth loss beyond a threshold, or latency and memory outside the target machine budget.</p>\n<h2>Make the decision reproducible</h2>\n<p>Package the base and candidate identities, dataset revision, evaluator revision, configuration, per-slice results, missing measurements, and final decision. Keep “ship” narrower than “the candidate improved.” A routed ship can be correct when the target gain is real and the routing constraint is enforced.</p>\n<p>PostTrainLLM includes fixture and no-GPU gates, live eval commands, canonical factory-run records, specialist packages, and public report cards. Several public results intentionally disclose missing historical latency, RAM, throughput, or raw traces rather than replacing those gaps with estimates.</p>\n<p><strong>CTA:</strong> Inspect the eval leaderboard and report cards, then use the factory recipes to reproduce the workflow.</p>\n"
  },
  "/agent-trajectory-dataset": {
    "title": "How to build an agent trajectory dataset",
    "description": "Build an agent trajectory dataset with typed events, provenance, privacy filters, leakage-safe splits, and outcome-based evaluation.",
    "path": "/agent-trajectory-dataset",
    "intent": "technical workflow for preparing agent trajectories for post-training",
    "schema": [
      "HowTo",
      "TechArticle",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/build-small-language-model-specialist",
      "/evaluate-local-llm",
      "/fine-tune-llm-on-mac",
      "/docs/factory"
    ],
    "markdown": "An agent trajectory dataset records more than the final answer. It preserves the task, observations, tool calls, tool results, decisions, corrections, and outcome so a specialist can learn the behavior that produced success. The dataset is only useful when its provenance, privacy, and evaluation boundary are explicit.\n\n## Define the training unit\n\nChoose whether one example is a full episode, one decision point, or a correction pair. Preserve the original task and enough state to understand each action. Avoid training on hidden evaluator feedback that the runtime will never receive.\n\n## Capture typed events\n\nNormalize observations, assistant reasoning that is permitted for training, tool names, arguments, results, errors, and final outputs into a versioned schema. Keep raw source references so the transformation can be audited. Do not flatten every event into an undifferentiated chat transcript.\n\n## Filter for useful behavior\n\nRemove duplicates, leaked tests, private values, malformed tool calls, and trajectories without an authoritative outcome. Separate successful examples from correction data and hard negatives. Preserve the reason an example was accepted or rejected.\n\n## Split by source and task family\n\nRandom row splits can leak nearly identical episodes across training and test sets. Group by repository, task template, session, or source artifact before splitting. Freeze the holdout before tuning the training recipe.\n\n## Train the narrowest useful target\n\nA trajectory dataset can support supervised fine-tuning, preference pairs, distillation, routing, or an evaluator. Choose the objective from the observed failure. If the model over-edits, an objective that increases edit pressure is the wrong response.\n\n## Evaluate the behavior, not imitation\n\nMeasure whether the specialist selects the correct tool, produces valid arguments, reacts to tool results, stops appropriately, and completes the task. Also measure unsupported actions and breadth regressions.\n\nPostTrainLLM includes trajectory conversion, correction-to-data, synthesis, quality filtering, deduplication, tool-calling evals, and factory-run evidence. Data and model training remain local and owner-controlled; public report cards should expose only safe, reviewed evidence.\n\n**CTA:** Continue to the specialist-model workflow or inspect the factory documentation.",
    "html": "<p>An agent trajectory dataset records more than the final answer. It preserves the task, observations, tool calls, tool results, decisions, corrections, and outcome so a specialist can learn the behavior that produced success. The dataset is only useful when its provenance, privacy, and evaluation boundary are explicit.</p>\n<h2>Define the training unit</h2>\n<p>Choose whether one example is a full episode, one decision point, or a correction pair. Preserve the original task and enough state to understand each action. Avoid training on hidden evaluator feedback that the runtime will never receive.</p>\n<h2>Capture typed events</h2>\n<p>Normalize observations, assistant reasoning that is permitted for training, tool names, arguments, results, errors, and final outputs into a versioned schema. Keep raw source references so the transformation can be audited. Do not flatten every event into an undifferentiated chat transcript.</p>\n<h2>Filter for useful behavior</h2>\n<p>Remove duplicates, leaked tests, private values, malformed tool calls, and trajectories without an authoritative outcome. Separate successful examples from correction data and hard negatives. Preserve the reason an example was accepted or rejected.</p>\n<h2>Split by source and task family</h2>\n<p>Random row splits can leak nearly identical episodes across training and test sets. Group by repository, task template, session, or source artifact before splitting. Freeze the holdout before tuning the training recipe.</p>\n<h2>Train the narrowest useful target</h2>\n<p>A trajectory dataset can support supervised fine-tuning, preference pairs, distillation, routing, or an evaluator. Choose the objective from the observed failure. If the model over-edits, an objective that increases edit pressure is the wrong response.</p>\n<h2>Evaluate the behavior, not imitation</h2>\n<p>Measure whether the specialist selects the correct tool, produces valid arguments, reacts to tool results, stops appropriately, and completes the task. Also measure unsupported actions and breadth regressions.</p>\n<p>PostTrainLLM includes trajectory conversion, correction-to-data, synthesis, quality filtering, deduplication, tool-calling evals, and factory-run evidence. Data and model training remain local and owner-controlled; public report cards should expose only safe, reviewed evidence.</p>\n<p><strong>CTA:</strong> Continue to the specialist-model workflow or inspect the factory documentation.</p>\n"
  },
  "/build-small-language-model-specialist": {
    "title": "How to build a small language model specialist",
    "description": "Build a routed small language model specialist with a frozen baseline, narrow training objective, regression gates, packaging, and an honest decision.",
    "path": "/build-small-language-model-specialist",
    "intent": "workflow for developers building task-specific small language models",
    "schema": [
      "HowTo",
      "BreadcrumbList"
    ],
    "internalLinks": [
      "/fine-tune-llm-on-mac",
      "/agent-trajectory-dataset",
      "/evaluate-local-llm",
      "/artifacts",
      "/recipes"
    ],
    "markdown": "A small language model specialist is a model that earns a narrow job through measured behavior. It does not need to outperform a frontier model everywhere. It needs to beat the chosen baseline on a defined target, stay inside regression and runtime limits, and route away when the task is outside its competence.\n\n## Pick the job\n\nGood specialist targets have repeatable inputs, objective outcomes, enough representative data, and a runtime reason to stay local. Examples include tool selection, structured extraction, file operations, SQL generation, formatting, or a bounded intent router.\n\n## Establish the baseline\n\nRun the unmodified base model on the frozen evaluation set before training. Record accuracy by slice, invalid outputs, latency, memory, and abstention or routing behavior when available. A specialist without a baseline has no demonstrated gain.\n\n## Choose the smallest intervention\n\nStart with prompting or constrained decoding when it solves the problem. Use LoRA or QLoRA when behavior must be learned. Consider distillation when a stronger teacher can generate reviewed target behavior. Do not stack techniques until the failure requires them.\n\n## Protect breadth with routing\n\nA specialist can regress on general tasks even while its target score improves. Add an explicit router or confidence boundary and evaluate it. The runtime should choose the specialist only for tasks it earned. Report a routed ship as routed, not as a new general default.\n\n## Package the whole result\n\nA useful specialist package includes the base identity, adapter or fused artifact, tokenizer and prompt contract, routing rule, runtime requirements, license information, evaluation report, known regressions, and reproducible configuration. Missing latency or memory remains missing.\n\n## Make a decision\n\nUse one of four honest outcomes: ship within the declared route, retry training, retry data or evaluation, or reject. Preserve failed attempts because they explain the next experiment and prevent the same recipe from being repeated without evidence.\n\nPostTrainLLM is organized around this target-to-report loop. Its public file-operations specialist is a concrete example of a strong target gain paired with a disclosed breadth regression and a routing constraint.\n\n**CTA:** Inspect the specialists and report cards, then follow the Mac fine-tuning workflow for your own target.",
    "html": "<p>A small language model specialist is a model that earns a narrow job through measured behavior. It does not need to outperform a frontier model everywhere. It needs to beat the chosen baseline on a defined target, stay inside regression and runtime limits, and route away when the task is outside its competence.</p>\n<h2>Pick the job</h2>\n<p>Good specialist targets have repeatable inputs, objective outcomes, enough representative data, and a runtime reason to stay local. Examples include tool selection, structured extraction, file operations, SQL generation, formatting, or a bounded intent router.</p>\n<h2>Establish the baseline</h2>\n<p>Run the unmodified base model on the frozen evaluation set before training. Record accuracy by slice, invalid outputs, latency, memory, and abstention or routing behavior when available. A specialist without a baseline has no demonstrated gain.</p>\n<h2>Choose the smallest intervention</h2>\n<p>Start with prompting or constrained decoding when it solves the problem. Use LoRA or QLoRA when behavior must be learned. Consider distillation when a stronger teacher can generate reviewed target behavior. Do not stack techniques until the failure requires them.</p>\n<h2>Protect breadth with routing</h2>\n<p>A specialist can regress on general tasks even while its target score improves. Add an explicit router or confidence boundary and evaluate it. The runtime should choose the specialist only for tasks it earned. Report a routed ship as routed, not as a new general default.</p>\n<h2>Package the whole result</h2>\n<p>A useful specialist package includes the base identity, adapter or fused artifact, tokenizer and prompt contract, routing rule, runtime requirements, license information, evaluation report, known regressions, and reproducible configuration. Missing latency or memory remains missing.</p>\n<h2>Make a decision</h2>\n<p>Use one of four honest outcomes: ship within the declared route, retry training, retry data or evaluation, or reject. Preserve failed attempts because they explain the next experiment and prevent the same recipe from being repeated without evidence.</p>\n<p>PostTrainLLM is organized around this target-to-report loop. Its public file-operations specialist is a concrete example of a strong target gain paired with a disclosed breadth regression and a routing constraint.</p>\n<p><strong>CTA:</strong> Inspect the specialists and report cards, then follow the Mac fine-tuning workflow for your own target.</p>\n"
  }
};

