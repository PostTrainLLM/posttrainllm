// Long-form articles recovered from marketing drafts (PR #154 source
// documents in marketing/articles/posttrainllm/). Same EditorialContent shape
// as education-content.ts; pages under src/pages/articles/ render these via
// components/content/EditorialPage.astro.
import type { EditorialContent } from "./education-content";

export const articleContent: Record<string, EditorialContent> = {
  "/articles/capability-per-gigabyte-ram-small-llms": {
    title:
      "Measuring Model Capability Per Gigabyte of RAM: Beyond Parameter Chasing",
    description:
      "Stop chasing model size. Learn how to evaluate local LLMs using capability per GB of RAM, active parameter efficiency, and hardware-aware density metrics.",
    path: "/articles/capability-per-gigabyte-ram-small-llms",
    intent:
      "Informational / Thought Leadership & Engineering Practice. System engineers, local AI developers, and hardware-constrained practitioners seeking cost-efficient LLM deployment strategies based on capability density (% frontier capability per GB RAM) rather than raw parameter count.",
    schema: ["TechArticle", "BreadcrumbList"],
    internalLinks: [
      "/articles",
      "/evaluate-local-llm",
      "/articles/lora-evaluation-workflow-mac-local-models",
      "/experiments",
      "/benchmarks",
      "/leaderboard",
    ],
    markdown:
      '## Introduction: The Parameter Count Paradigm Trap\n\nFor years, the open-source language model ecosystem has been driven by parameter count inflation. Benchmark leaderboards routinely highlight 70B, 120B, or 405B parameter models. However, for practitioners building applications designed to run locally on workstations, edge devices, or user laptops, raw parameter count is a misleading and expensive metric.\n\nOn hardware with fixed memory budgets—such as Apple Silicon MacBooks or embedded edge devices—every gigabyte of RAM consumed by model weights is a gigabyte unavailable for KV caching, system UI compositing, user application state, or parallel worker threads. The true engineering goal for local AI is not to train the largest possible model, but to achieve maximum **Capability Density**: maximizing task completion per gigabyte of RAM used.\n\nWhen documenting capability density, it is crucial to categorize information accurately:\n- **General Guidance:** System design principles for memory-constrained local execution.\n- **Recipe Contracts:** Resource-bounded execution specs (RAM allocation limits, context window sizes, quantization precision).\n- **Public Artifacts:** Specialist model packages carrying explicit memory and throughput metadata.\n- **Actual Runs:** Hardware-measured empirical benchmarks (Peak RSS, tok/s, TTFT) from local test harnesses.\n\n## The Core Metric: % Frontier Capability Retained Per Unit of RAM\n\nInstead of treating frontier models (such as Claude 3.5 Sonnet or GPT-4o / Codex) as rivals to beat locally, local LLM architecture treats them as **calibration anchors**. The headline metric for local model efficiency is defined as:\n\n$$\\text{Capability Density} = \\frac{\\% \\text{ Frontier Capability Retained}}{\\text{Peak RAM / Memory Footprint (GB)}}$$\n\n```text\n               ┌─────────────────────────────────────────────────────────┐\n               │              Frontier Calibration Anchor                │\n               │                   (~100% Benchmark Ceiling)             │\n               └────────────────────────────┬────────────────────────────┘\n                                            │\n                                            ▼\n               ┌─────────────────────────────────────────────────────────┐\n               │           Mac-Local Candidate Measurement              │\n               │   Score: % Frontier Retained  │  Footprint: Peak RSS GB │\n               └────────────────────────────┬────────────────────────────┘\n                                            │\n                                            ▼\n               ┌─────────────────────────────────────────────────────────┐\n               │    Capability Density = % Retained / Peak RSS (GB)      │\n               └─────────────────────────────────────────────────────────┘\n```\n\n### The Physics of Unified Memory Architecture (UMA)\nApple Silicon integrates CPU, GPU, and Neural Engine access into a single, high-bandwidth Unified Memory pool. While UMA eliminates PCIe transfer bottlenecks, memory pressure remains the hard ceiling for local execution.\n\nWhen a model runs, its operational memory footprint consists of:\n1. **Static Weight Footprint:** Model parameters loaded in memory (e.g., 4B parameters in 4-bit quantization consume ~2.5 GB).\n2. **Dynamic Activation & KV Cache Footprint:** Context window memory scaling linearly or quadratically with sequence length and batch size.\n3. **Peak Resident Set Size (Peak RSS):** The maximum physical RAM allocated by macOS during active inference or post-training.\n\nIf Peak RSS exceeds available system memory, macOS initiates paging to SSD swap, causing token generation rates to drop from 50+ tok/s to sub-1 tok/s.\n\n## Active Parameter Efficiency: MoE and Distillation Case Studies\n\nAchieving high capability density relies on two primary techniques: sparse active parameter execution and domain-specific knowledge distillation.\n\n### 1. Mixture-of-Experts (MoE) Active Efficiency\nMixture-of-Experts (MoE) architectures separate total parameter footprint from per-token compute cost. A prominent example evaluated on Mac hardware is **Qwen3-30B-A3B**:\n- **Total Footprint:** ~30B parameters (~18 GB RAM in 4-bit quantization).\n- **Active Parameters:** ~3B active parameters per token.\n- **Empirical Performance:** On multi-turn agentic function calling (`parallel` and `parallel_multiple` tool selection), this model reaches **~96% accuracy**, matching frontier performance. By activating only ~3B parameters per forward pass, it achieves frontier-level tool selection at a fraction of the compute cost of a dense 30B model.\n\n### 2. Distilled Small Model Specialists\nWhen total RAM is constrained to under 8 GB (typical for consumer MacBooks running background developer utilities), dense 1.7B and 4B models distilled from larger teachers offer extreme capability density:\n\n| Model / Package | Active Params | Peak RSS Footprint | Target Task Depth | General Breadth | Capability Density Assessment |\n| :--- | :--- | :--- | :--- | :--- | :--- |\n| **Qwen3-4B File-Ops Distilled** | 4.0B | ~4.2 GB | **100.0%** (File-Ops) | 42.3% | **High Specialist Density:** Perfect file action grounding within 4.2 GB RAM. |\n| **Qwen3-4B ReST Fused** | 4.0B | ~4.2 GB | **100.0%** (File-Ops) | 55.5% | **Balanced Specialist Density:** 2.42× faster execution wall time with reduced side effects. |\n| **Qwen3-0.6B SQL Specialist** | 0.6B | ~1.1 GB | **92.0%** (SQL Exec) | 38.0% | **Extreme Footprint Efficiency:** High single-domain execution accuracy in ~1.1 GB RAM. |\n\n## On-Device Foundation Model Assessment: Apple FM vs. Owned Weights\n\nA key question for macOS developers is whether to rely on Apple\'s built-in on-device Foundation Models (exposed via system APIs) or deploy owned model weights (e.g., Qwen or Gemma variants running over local MLX/Metal engines).\n\nEmpirical probing of Apple\'s on-device Foundation Model (`scripts/fm_agent_bridge.swift`) revealed severe capability density limitations:\n\n```text\n[ Task: Action Grounding / Tool Calling ]\n ├── Apple On-Device Foundation Model ──> BFCL Agentic: 25% (Full) / ~0% (Compact) ──> Context Ceiling: 4,096 tokens\n └── Owned Qwen3-4B Specialist (MLX)  ──> BFCL Agentic: 85%+              ──> Context Ceiling: 32,768+ tokens\n```\n\n### Key Empirical Findings:\n1. **Action-Grounding Failure:** Apple\'s on-device model achieved only **25%** on full-catalog BFCL agentic tool selection and **~0%** on compact tool selections. On planner action-grounding tasks, it scored **13%**.\n2. **Context Window Bottleneck:** The built-in model\'s strict 4,096-token context window cannot hold a realistic developer tool catalog alongside multi-turn conversation history.\n3. **Execution Speed:** The system model did not generate tokens significantly faster than a dedicated, 4-bit quantized 4B model running on MLX.\n\n**Strategic Principle:** Own the model; do not depend on system runtime models for core application capabilities. Treat system models strictly as free routing floors. Core ML compilation of owned weights remains an optional future battery optimization target, not a capability dependency.\n\n## Browser & WebGPU Execution Limits\n\nCapability density also governs in-browser AI execution. When running client-side models via WASM or WebGPU, memory allocation limits and browser sandboxing create strict operational boundaries.\n\nEmpirical ABBA benchmark measurements on an Apple M5 Pro / Metal 3 setup using the posttrainllm browser runtime provided concrete proof of WebGPU capability density:\n- **Model Preset:** Large preset model.\n- **Execution Speedup:** WebGPU achieved a **10.67× median speedup** over WASM SIMD execution.\n- **Numerical Fidelity:** Final loss drift between WebGPU and WASM stayed under **4.72%** with zero runtime allocation errors.\n\nHowever, browser WASM runtimes encounter severe Memory64 Out-Of-Bounds (OOB) allocation failures when attempting to load models above 2B parameters without custom shared-memory growth handlers.\n\n## Accounting for Density in Fine-Tune Report Cards\n\nTo enforce capability density in local deployment, every model fine-tuning run must emit a standardized Fine-Tune Report Card (`report-card.json`). The report card mandates tracking resource utilization alongside accuracy metrics:\n\n```json\n{\n  "artifact_name": "qwen3-4b-rest-fused",\n  "decision": "routed-ship",\n  "metrics": {\n    "target_depth_score": 1.00,\n    "general_breadth_score": 0.555,\n    "peak_rss_bytes": 4509715456,\n    "decode_tok_per_sec": 42.5,\n    "time_to_first_token_ms": 112.4\n  },\n  "measurement_states": {\n    "peak_rss_bytes": "measured",\n    "decode_tok_per_sec": "measured"\n  }\n}\n```\n\nIf a candidate model increases RAM consumption by 300% (e.g., jumping from 4B to 14B parameters) while improving target task accuracy by only 2%, the capability density decreases—justifying a `reject` or `routed-ship` decision in favor of the smaller, higher-density model.\n\n## Clear Next Action\n\nTo measure capability density for your local models:\n1. Measure your base model\'s baseline Peak RSS and decode throughput using `posttrainllm serve --metrics`.\n2. Evaluate target task depth and out-of-domain breadth on your 1.7B or 4B candidate.\n3. Compute the capability density ratio (% frontier capability retained per GB of Peak RSS).\n4. Build a fine-tune report card using `python3 scripts/factory/build_fine_tune_report_card.py`.\n5. Select the smallest, highest-density candidate that satisfies your application\'s task-completion threshold.',
    html: "<h2>Introduction: The Parameter Count Paradigm Trap</h2>\n<p>For years, the open-source language model ecosystem has been driven by parameter count inflation. Benchmark leaderboards routinely highlight 70B, 120B, or 405B parameter models. However, for practitioners building applications designed to run locally on workstations, edge devices, or user laptops, raw parameter count is a misleading and expensive metric.</p>\n<p>On hardware with fixed memory budgets—such as Apple Silicon MacBooks or embedded edge devices—every gigabyte of RAM consumed by model weights is a gigabyte unavailable for KV caching, system UI compositing, user application state, or parallel worker threads. The true engineering goal for local AI is not to train the largest possible model, but to achieve maximum <strong>Capability Density</strong>: maximizing task completion per gigabyte of RAM used.</p>\n<p>When documenting capability density, it is crucial to categorize information accurately:</p>\n<ul><li><strong>General Guidance:</strong> System design principles for memory-constrained local execution.</li>\n<li><strong>Recipe Contracts:</strong> Resource-bounded execution specs (RAM allocation limits, context window sizes, quantization precision).</li>\n<li><strong>Public Artifacts:</strong> Specialist model packages carrying explicit memory and throughput metadata.</li>\n<li><strong>Actual Runs:</strong> Hardware-measured empirical benchmarks (Peak RSS, tok/s, TTFT) from local test harnesses.</li></ul>\n<h2>The Core Metric: % Frontier Capability Retained Per Unit of RAM</h2>\n<p>Instead of treating frontier models (such as Claude 3.5 Sonnet or GPT-4o / Codex) as rivals to beat locally, local LLM architecture treats them as <strong>calibration anchors</strong>. The headline metric for local model efficiency is defined as:</p>\n<pre><code>Capability Density = (% Frontier Capability Retained) / (Peak RAM / Memory Footprint (GB))</code></pre>\n<pre><code>               ┌─────────────────────────────────────────────────────────┐\n               │              Frontier Calibration Anchor                │\n               │                   (~100% Benchmark Ceiling)             │\n               └────────────────────────────┬────────────────────────────┘\n                                            │\n                                            ▼\n               ┌─────────────────────────────────────────────────────────┐\n               │           Mac-Local Candidate Measurement              │\n               │   Score: % Frontier Retained  │  Footprint: Peak RSS GB │\n               └────────────────────────────┬────────────────────────────┘\n                                            │\n                                            ▼\n               ┌─────────────────────────────────────────────────────────┐\n               │    Capability Density = % Retained / Peak RSS (GB)      │\n               └─────────────────────────────────────────────────────────┘</code></pre>\n<h3>The Physics of Unified Memory Architecture (UMA)</h3>\n<p>Apple Silicon integrates CPU, GPU, and Neural Engine access into a single, high-bandwidth Unified Memory pool. While UMA eliminates PCIe transfer bottlenecks, memory pressure remains the hard ceiling for local execution.</p>\n<p>When a model runs, its operational memory footprint consists of:</p>\n<ol><li><strong>Static Weight Footprint:</strong> Model parameters loaded in memory (e.g., 4B parameters in 4-bit quantization consume ~2.5 GB).</li>\n<li><strong>Dynamic Activation &amp; KV Cache Footprint:</strong> Context window memory scaling linearly or quadratically with sequence length and batch size.</li>\n<li><strong>Peak Resident Set Size (Peak RSS):</strong> The maximum physical RAM allocated by macOS during active inference or post-training.</li></ol>\n<p>If Peak RSS exceeds available system memory, macOS initiates paging to SSD swap, causing token generation rates to drop from 50+ tok/s to sub-1 tok/s.</p>\n<h2>Active Parameter Efficiency: MoE and Distillation Case Studies</h2>\n<p>Achieving high capability density relies on two primary techniques: sparse active parameter execution and domain-specific knowledge distillation.</p>\n<h3>1. Mixture-of-Experts (MoE) Active Efficiency</h3>\n<p>Mixture-of-Experts (MoE) architectures separate total parameter footprint from per-token compute cost. A prominent example evaluated on Mac hardware is <strong>Qwen3-30B-A3B</strong>:</p>\n<ul><li><strong>Total Footprint:</strong> ~30B parameters (~18 GB RAM in 4-bit quantization).</li>\n<li><strong>Active Parameters:</strong> ~3B active parameters per token.</li>\n<li><strong>Empirical Performance:</strong> On multi-turn agentic function calling (<code>parallel</code> and <code>parallel_multiple</code> tool selection), this model reaches <strong>~96% accuracy</strong>, matching frontier performance. By activating only ~3B parameters per forward pass, it achieves frontier-level tool selection at a fraction of the compute cost of a dense 30B model.</li></ul>\n<h3>2. Distilled Small Model Specialists</h3>\n<p>When total RAM is constrained to under 8 GB (typical for consumer MacBooks running background developer utilities), dense 1.7B and 4B models distilled from larger teachers offer extreme capability density:</p>\n<table><thead><tr><th>Model / Package</th><th>Active Params</th><th>Peak RSS Footprint</th><th>Target Task Depth</th><th>General Breadth</th><th>Capability Density Assessment</th></tr></thead><tbody><tr><td><strong>Qwen3-4B File-Ops Distilled</strong></td><td>4.0B</td><td>~4.2 GB</td><td><strong>100.0%</strong> (File-Ops)</td><td>42.3%</td><td><strong>High Specialist Density:</strong> Perfect file action grounding within 4.2 GB RAM.</td></tr><tr><td><strong>Qwen3-4B ReST Fused</strong></td><td>4.0B</td><td>~4.2 GB</td><td><strong>100.0%</strong> (File-Ops)</td><td>55.5%</td><td><strong>Balanced Specialist Density:</strong> 2.42× faster execution wall time with reduced side effects.</td></tr><tr><td><strong>Qwen3-0.6B SQL Specialist</strong></td><td>0.6B</td><td>~1.1 GB</td><td><strong>92.0%</strong> (SQL Exec)</td><td>38.0%</td><td><strong>Extreme Footprint Efficiency:</strong> High single-domain execution accuracy in ~1.1 GB RAM.</td></tr></tbody></table>\n<h2>On-Device Foundation Model Assessment: Apple FM vs. Owned Weights</h2>\n<p>A key question for macOS developers is whether to rely on Apple's built-in on-device Foundation Models (exposed via system APIs) or deploy owned model weights (e.g., Qwen or Gemma variants running over local MLX/Metal engines).</p>\n<p>Empirical probing of Apple's on-device Foundation Model (<code>scripts/fm_agent_bridge.swift</code>) revealed severe capability density limitations:</p>\n<pre><code>[ Task: Action Grounding / Tool Calling ]\n ├── Apple On-Device Foundation Model ──&gt; BFCL Agentic: 25% (Full) / ~0% (Compact) ──&gt; Context Ceiling: 4,096 tokens\n └── Owned Qwen3-4B Specialist (MLX)  ──&gt; BFCL Agentic: 85%+              ──&gt; Context Ceiling: 32,768+ tokens</code></pre>\n<h3>Key Empirical Findings:</h3>\n<ol><li><strong>Action-Grounding Failure:</strong> Apple's on-device model achieved only <strong>25%</strong> on full-catalog BFCL agentic tool selection and <strong>~0%</strong> on compact tool selections. On planner action-grounding tasks, it scored <strong>13%</strong>.</li>\n<li><strong>Context Window Bottleneck:</strong> The built-in model's strict 4,096-token context window cannot hold a realistic developer tool catalog alongside multi-turn conversation history.</li>\n<li><strong>Execution Speed:</strong> The system model did not generate tokens significantly faster than a dedicated, 4-bit quantized 4B model running on MLX.</li></ol>\n<p><strong>Strategic Principle:</strong> Own the model; do not depend on system runtime models for core application capabilities. Treat system models strictly as free routing floors. Core ML compilation of owned weights remains an optional future battery optimization target, not a capability dependency.</p>\n<h2>Browser &amp; WebGPU Execution Limits</h2>\n<p>Capability density also governs in-browser AI execution. When running client-side models via WASM or WebGPU, memory allocation limits and browser sandboxing create strict operational boundaries.</p>\n<p>Empirical ABBA benchmark measurements on an Apple M5 Pro / Metal 3 setup using the posttrainllm browser runtime provided concrete proof of WebGPU capability density:</p>\n<ul><li><strong>Model Preset:</strong> Large preset model.</li>\n<li><strong>Execution Speedup:</strong> WebGPU achieved a <strong>10.67× median speedup</strong> over WASM SIMD execution.</li>\n<li><strong>Numerical Fidelity:</strong> Final loss drift between WebGPU and WASM stayed under <strong>4.72%</strong> with zero runtime allocation errors.</li></ul>\n<p>However, browser WASM runtimes encounter severe Memory64 Out-Of-Bounds (OOB) allocation failures when attempting to load models above 2B parameters without custom shared-memory growth handlers.</p>\n<h2>Accounting for Density in Fine-Tune Report Cards</h2>\n<p>To enforce capability density in local deployment, every model fine-tuning run must emit a standardized Fine-Tune Report Card (<code>report-card.json</code>). The report card mandates tracking resource utilization alongside accuracy metrics:</p>\n<pre><code>{\n  &quot;artifact_name&quot;: &quot;qwen3-4b-rest-fused&quot;,\n  &quot;decision&quot;: &quot;routed-ship&quot;,\n  &quot;metrics&quot;: {\n    &quot;target_depth_score&quot;: 1.00,\n    &quot;general_breadth_score&quot;: 0.555,\n    &quot;peak_rss_bytes&quot;: 4509715456,\n    &quot;decode_tok_per_sec&quot;: 42.5,\n    &quot;time_to_first_token_ms&quot;: 112.4\n  },\n  &quot;measurement_states&quot;: {\n    &quot;peak_rss_bytes&quot;: &quot;measured&quot;,\n    &quot;decode_tok_per_sec&quot;: &quot;measured&quot;\n  }\n}</code></pre>\n<p>If a candidate model increases RAM consumption by 300% (e.g., jumping from 4B to 14B parameters) while improving target task accuracy by only 2%, the capability density decreases—justifying a <code>reject</code> or <code>routed-ship</code> decision in favor of the smaller, higher-density model.</p>\n<h2>Clear Next Action</h2>\n<p>To measure capability density for your local models:</p>\n<ol><li>Measure your base model's baseline Peak RSS and decode throughput using <code>posttrainllm serve --metrics</code>.</li>\n<li>Evaluate target task depth and out-of-domain breadth on your 1.7B or 4B candidate.</li>\n<li>Compute the capability density ratio (% frontier capability retained per GB of Peak RSS).</li>\n<li>Build a fine-tune report card using <code>python3 scripts/factory/build_fine_tune_report_card.py</code>.</li>\n<li>Select the smallest, highest-density candidate that satisfies your application's task-completion threshold.</li></ol>",
  },
  "/articles/how-to-compare-two-fine-tuning-runs-without-moving-the-evaluation-target":
    {
      title:
        "How to compare two fine-tuning runs without moving the evaluation target",
      description:
        "Learn how to compare LLM fine-tuning runs effectively by freezing baselines, using frontier-calibrated gates, and measuring both primary and regression scores.",
      path: "/articles/how-to-compare-two-fine-tuning-runs-without-moving-the-evaluation-target",
      intent: "Informational",
      schema: ["TechArticle", "BreadcrumbList"],
      internalLinks: [
        "/articles",
        "/evaluate-local-llm",
        "/articles/lora-evaluation-workflow-mac-local-models",
        "/experiments",
        "/recipes",
        "/learn",
      ],
      markdown:
        "## Introduction\n\nEvaluating a fine-tuned language model is deceptively difficult. When you change hyperparameters, swap a dataset, or move from a 1.7B parameter base model to a 4B parameter model, you expect a clear signal of improvement or degradation. However, how do you know if the new model is genuinely better? If the evaluation metric changes alongside the model, or if the test itself is structurally flawed, you might be measuring noise instead of a genuine capability improvement.\n\nIn the PostTrainLLM research factory—a project dedicated to building and evaluating Mac-local models on Apple Silicon—we discovered early on that improving a model's score often means fixing the evaluation first. The goal is not merely to build models but to understand the space rigorously, establishing a repeatable factory loop of `target -> data -> post-training -> eval -> package -> report`.\n\nThis article outlines an evidence-backed protocol for comparing two fine-tuning runs without moving the evaluation target, ensuring that every point of improvement is real. Whether you are running parameter-efficient fine-tuning (PEFT) on a local machine or managing larger clusters, these principles will help you separate actual progress from evaluation artifacts. To see how these principles apply in practice across our guided learning paths, refer to our [learning labs](/learn).\n\n## Rule 1: The Frozen Baseline\n\nThe first and most critical rule of comparing fine-tuning runs is to freeze the evaluation *before* training begins. A frozen baseline consists of a fixed set of prompts, a fixed scoring script, and a fixed reference performance from the base model. If you adjust the evaluation dataset or modify the scoring script after training a candidate model, you invalidate the comparison. Every fine-tuning run must be evaluated against the exact same frozen baseline to produce a meaningful delta.\n\nWhen you run a baseline evaluation first, you establish the objective starting point. For instance, if your stock Qwen3-4B model scores 87.3% on a single-turn tool-calling benchmark, that is your frozen baseline. Any subsequent experiment—whether applying ToolRL-GRPO, function-masking SFT, or a new distillation recipe—must be judged against that 87.3%. If the new score drops to 80.0%, the run regressed. That is a measurable, honest result that prevents self-deception.\n\nThis rule is load-bearing. In the PostTrainLLM factory loop, the baseline comes strictly before training. This ensures that any reported improvement is entirely attributable to the post-training process, not to a coincidentally easier evaluation slice or a more lenient scoring script introduced after the fact. Review our past results in our [experiment archive](/experiments) to see how frozen baselines anchor our evaluations.\n\n## Rule 2: The Frontier-Ceiling Gate\n\nA common pitfall in evaluating small models is using a benchmark that is fundamentally broken. If an evaluation task asks for information that cannot be logically derived from the prompt, no model—regardless of size or training budget—will score well.\n\nThis leads to the **frontier-ceiling gate**: before any benchmark is used to grade a small, locally trained model, a frontier model must score approximately 100% on it. If a frontier model cannot ace the benchmark, the benchmark is broken. You must fix it or drop it entirely; you should never report a small model's accuracy on a benchmark that fails this calibration.\n\nA clear example of this occurred when evaluating tool-calling capabilities. Initially, the `hermes-fc` dataset was used to grade local models. The local 1.7B model scored around 55% using exact-string matching. However, a deeper analysis revealed that approximately 29% of the held-out examples required \"ungroundable\" arguments—values such as specific device IDs, transaction codes, or literal placeholder strings like `unique_nft_identifier` that appeared nowhere in the prompt. When a frontier model was tested on these hard cases, it scored only around 12%, and notably, its answers were often more logically correct than the established gold standard.\n\nBecause the frontier model failed the ceiling gate, the `hermes-fc` dataset was strictly relegated to training data and disqualified as a reporting metric. The evaluation target shifted to the Berkeley Function Calling Leaderboard (BFCL) suite, where golds are verified as groundable. When tested against a controlled single-turn harness derived from BFCL, a frontier model scored 99.2%, proving it was a valid, reliable ruler for comparing smaller models.\n\nBy using a frontier-calibrated benchmark, you ensure that the gap you measure between two fine-tuning runs reflects a real capability difference. The frontier model serves as the calibration anchor, ensuring you are measuring against a reachable capability ceiling.\n\n## Rule 3: Measuring the Breadth Tax (Catastrophic Forgetting)\n\nFine-tuning is rarely a free lunch. Specializing a model for a specific task often causes it to forget general capabilities—a phenomenon known as catastrophic forgetting, negative transfer, or the \"breadth tax.\" To accurately compare two runs, you must separate the primary task score (depth) from the regression score (breadth).\n\nConsider a multi-turn agentic task focused specifically on file-system operations. A stock 4B parameter model might score 58% on this difficult gate. After applying rejection-sampling distillation using 99 high-quality frontier trajectories, a specialized 4B model achieved 100% on the exact same gate. Viewed in isolation, this looks like an unmitigated success for the primary task.\n\nHowever, the evaluation must not stop there. When the same models were tested on 52 held-out, out-of-domain tasks (spanning trading bots, vehicle control APIs, and travel bookings), the stock model scored 59.6%. The narrowly distilled model, which aced the file-ops tasks, saw its out-of-domain score drop significantly to 42.3%.\n\nThe fine-tuning run bought depth in file operations at the cost of a 17-point regression in breadth. The model became a file-ops specialist, not a generally better agent.\n\nWhen comparing runs, you must report both numbers. A report card that only shows the 100% depth win is incomplete and misleading. By rigorously tracking both primary and regression scores, you can make an informed decision on how to deploy the artifact: you might choose to route the model specifically for its narrow domain (as a routed specialist), or you might iterate on the training data to recover breadth through techniques like interleaved multi-backend distillation. For more on creating and utilizing these models, refer to our [recipe registry](/recipes).\n\n## Rule 4: Trusting the Parser, Not the String\n\nThe mechanism used to score the model is just as important as the dataset itself. Exact-string matching is notoriously brittle and punitive for evaluating language models, especially for structured outputs like tool calls, JSON payloads, or code snippets.\n\nTo compare fine-tuning runs fairly and accurately, prefer benchmarks that utilize verified golds combined with Abstract Syntax Tree (AST) matching, semantic matching, and per-call partial credit. A lenient, well-tested parser is an absolute prerequisite for a fair evaluation.\n\nDuring the development of our tool-calling benchmarks, two subtle parser bugs initially masked the true capabilities of the local models. One bug inadvertently discarded multi-call outputs that used a single closing tag, while another failed to properly parse bare-JSON calls containing nested arguments.\n\nFixing these parser bugs had a dramatic impact on the measured results. For example, a distilled 1.7B model's score on parallel multiple calls moved from a false 8% to a real 60% simply by correcting the parsing logic.\n\nWhen comparing two runs, ensure your parser evaluates the semantic correctness and functional validity of the output, not just its exact formatting or character sequence. If Run A appears worse than Run B, verify that the parser isn't unfairly penalizing Run A for a functionally valid, but slightly structurally different, output. A rigorous evaluation requires measuring intent and execution, not just syntax.\n\n## Rule 5: Enforcing Ship, Retry, and Reject Decisions\n\nThe ultimate goal of comparing fine-tuning runs is not just to generate metrics, but to make decisions. An effective evaluation protocol must enforce explicit outcomes: Ship, Retry, or Reject. Optimistic prose about a model's potential cannot replace a hard decision based on the numbers.\n\n*   **Ship:** You ship a candidate only when the primary score clearly beats the frozen baseline, the regression is within acceptable limits, the failure classes are well-understood, and the entire artifact can be reproduced.\n*   **Retry:** You decide to retry (e.g., retry-data or retry-training) when the model shows promise but fails a critical constraint. For instance, a DPO run that improves execution scores but fails to fix output-format hygiene requires a data retry, not a ship.\n*   **Reject:** You reject a candidate when the primary score does not beat the baseline, when breadth damage erases the gain, or when the evaluation itself is deemed too unstable to trust. Rejecting a model is a successful application of the evaluation protocol.\n\nBy strictly enforcing these decisions, you ensure that every fine-tuning run either improves the system or provides a documented learning outcome, avoiding the accumulation of untrusted or marginally useful models.\n\n## Summary of the Protocol\n\nTo effectively compare two fine-tuning runs without moving the evaluation target, adhere to these core principles:\n\n1.  **Freeze the Eval**: Define your dataset, your parser, and your baseline score *before* any training code is executed.\n2.  **Calibrate with Frontier**: Ensure a frontier model can achieve near 100% on your benchmark. If it cannot, you are optimizing against noise.\n3.  **Measure the Trade-off**: Always report both the primary task score (depth) and a regression/breadth score to accurately capture catastrophic forgetting.\n4.  **Use Semantic Matching**: Avoid brittle exact-string match; employ AST parsing or semantic evaluation to measure true capability.\n5.  **Force a Decision**: Use the evaluation data to make a clear Ship, Retry, or Reject decision, recording all metrics in a durable report.\n\nBy adopting this evidence-backed protocol, you can trust your evaluation metrics and make confident decisions about the value of your fine-tuning experiments.\n\n## Next Action\n\nTo apply these principles today, start by defining a completely frozen baseline for your current project. Select a small, groundable dataset tailored to your primary task. Run a frontier model against this dataset to confirm a >95% ceiling. Then, evaluate your stock base model to establish your starting score. Only after these numbers are recorded should you begin your next fine-tuning experiment.",
      html: '<h2>Introduction</h2>\n<p>Evaluating a fine-tuned language model is deceptively difficult. When you change hyperparameters, swap a dataset, or move from a 1.7B parameter base model to a 4B parameter model, you expect a clear signal of improvement or degradation. However, how do you know if the new model is genuinely better? If the evaluation metric changes alongside the model, or if the test itself is structurally flawed, you might be measuring noise instead of a genuine capability improvement.</p>\n<p>In the PostTrainLLM research factory—a project dedicated to building and evaluating Mac-local models on Apple Silicon—we discovered early on that improving a model\'s score often means fixing the evaluation first. The goal is not merely to build models but to understand the space rigorously, establishing a repeatable factory loop of <code>target -&gt; data -&gt; post-training -&gt; eval -&gt; package -&gt; report</code>.</p>\n<p>This article outlines an evidence-backed protocol for comparing two fine-tuning runs without moving the evaluation target, ensuring that every point of improvement is real. Whether you are running parameter-efficient fine-tuning (PEFT) on a local machine or managing larger clusters, these principles will help you separate actual progress from evaluation artifacts. To see how these principles apply in practice across our guided learning paths, refer to our <a href="/learn">learning labs</a>.</p>\n<h2>Rule 1: The Frozen Baseline</h2>\n<p>The first and most critical rule of comparing fine-tuning runs is to freeze the evaluation <em>before</em> training begins. A frozen baseline consists of a fixed set of prompts, a fixed scoring script, and a fixed reference performance from the base model. If you adjust the evaluation dataset or modify the scoring script after training a candidate model, you invalidate the comparison. Every fine-tuning run must be evaluated against the exact same frozen baseline to produce a meaningful delta.</p>\n<p>When you run a baseline evaluation first, you establish the objective starting point. For instance, if your stock Qwen3-4B model scores 87.3% on a single-turn tool-calling benchmark, that is your frozen baseline. Any subsequent experiment—whether applying ToolRL-GRPO, function-masking SFT, or a new distillation recipe—must be judged against that 87.3%. If the new score drops to 80.0%, the run regressed. That is a measurable, honest result that prevents self-deception.</p>\n<p>This rule is load-bearing. In the PostTrainLLM factory loop, the baseline comes strictly before training. This ensures that any reported improvement is entirely attributable to the post-training process, not to a coincidentally easier evaluation slice or a more lenient scoring script introduced after the fact. Review our past results in our <a href="/experiments">experiment archive</a> to see how frozen baselines anchor our evaluations.</p>\n<h2>Rule 2: The Frontier-Ceiling Gate</h2>\n<p>A common pitfall in evaluating small models is using a benchmark that is fundamentally broken. If an evaluation task asks for information that cannot be logically derived from the prompt, no model—regardless of size or training budget—will score well.</p>\n<p>This leads to the <strong>frontier-ceiling gate</strong>: before any benchmark is used to grade a small, locally trained model, a frontier model must score approximately 100% on it. If a frontier model cannot ace the benchmark, the benchmark is broken. You must fix it or drop it entirely; you should never report a small model\'s accuracy on a benchmark that fails this calibration.</p>\n<p>A clear example of this occurred when evaluating tool-calling capabilities. Initially, the <code>hermes-fc</code> dataset was used to grade local models. The local 1.7B model scored around 55% using exact-string matching. However, a deeper analysis revealed that approximately 29% of the held-out examples required &quot;ungroundable&quot; arguments—values such as specific device IDs, transaction codes, or literal placeholder strings like <code>unique_nft_identifier</code> that appeared nowhere in the prompt. When a frontier model was tested on these hard cases, it scored only around 12%, and notably, its answers were often more logically correct than the established gold standard.</p>\n<p>Because the frontier model failed the ceiling gate, the <code>hermes-fc</code> dataset was strictly relegated to training data and disqualified as a reporting metric. The evaluation target shifted to the Berkeley Function Calling Leaderboard (BFCL) suite, where golds are verified as groundable. When tested against a controlled single-turn harness derived from BFCL, a frontier model scored 99.2%, proving it was a valid, reliable ruler for comparing smaller models.</p>\n<p>By using a frontier-calibrated benchmark, you ensure that the gap you measure between two fine-tuning runs reflects a real capability difference. The frontier model serves as the calibration anchor, ensuring you are measuring against a reachable capability ceiling.</p>\n<h2>Rule 3: Measuring the Breadth Tax (Catastrophic Forgetting)</h2>\n<p>Fine-tuning is rarely a free lunch. Specializing a model for a specific task often causes it to forget general capabilities—a phenomenon known as catastrophic forgetting, negative transfer, or the &quot;breadth tax.&quot; To accurately compare two runs, you must separate the primary task score (depth) from the regression score (breadth).</p>\n<p>Consider a multi-turn agentic task focused specifically on file-system operations. A stock 4B parameter model might score 58% on this difficult gate. After applying rejection-sampling distillation using 99 high-quality frontier trajectories, a specialized 4B model achieved 100% on the exact same gate. Viewed in isolation, this looks like an unmitigated success for the primary task.</p>\n<p>However, the evaluation must not stop there. When the same models were tested on 52 held-out, out-of-domain tasks (spanning trading bots, vehicle control APIs, and travel bookings), the stock model scored 59.6%. The narrowly distilled model, which aced the file-ops tasks, saw its out-of-domain score drop significantly to 42.3%.</p>\n<p>The fine-tuning run bought depth in file operations at the cost of a 17-point regression in breadth. The model became a file-ops specialist, not a generally better agent.</p>\n<p>When comparing runs, you must report both numbers. A report card that only shows the 100% depth win is incomplete and misleading. By rigorously tracking both primary and regression scores, you can make an informed decision on how to deploy the artifact: you might choose to route the model specifically for its narrow domain (as a routed specialist), or you might iterate on the training data to recover breadth through techniques like interleaved multi-backend distillation. For more on creating and utilizing these models, refer to our <a href="/recipes">recipe registry</a>.</p>\n<h2>Rule 4: Trusting the Parser, Not the String</h2>\n<p>The mechanism used to score the model is just as important as the dataset itself. Exact-string matching is notoriously brittle and punitive for evaluating language models, especially for structured outputs like tool calls, JSON payloads, or code snippets.</p>\n<p>To compare fine-tuning runs fairly and accurately, prefer benchmarks that utilize verified golds combined with Abstract Syntax Tree (AST) matching, semantic matching, and per-call partial credit. A lenient, well-tested parser is an absolute prerequisite for a fair evaluation.</p>\n<p>During the development of our tool-calling benchmarks, two subtle parser bugs initially masked the true capabilities of the local models. One bug inadvertently discarded multi-call outputs that used a single closing tag, while another failed to properly parse bare-JSON calls containing nested arguments.</p>\n<p>Fixing these parser bugs had a dramatic impact on the measured results. For example, a distilled 1.7B model\'s score on parallel multiple calls moved from a false 8% to a real 60% simply by correcting the parsing logic.</p>\n<p>When comparing two runs, ensure your parser evaluates the semantic correctness and functional validity of the output, not just its exact formatting or character sequence. If Run A appears worse than Run B, verify that the parser isn\'t unfairly penalizing Run A for a functionally valid, but slightly structurally different, output. A rigorous evaluation requires measuring intent and execution, not just syntax.</p>\n<h2>Rule 5: Enforcing Ship, Retry, and Reject Decisions</h2>\n<p>The ultimate goal of comparing fine-tuning runs is not just to generate metrics, but to make decisions. An effective evaluation protocol must enforce explicit outcomes: Ship, Retry, or Reject. Optimistic prose about a model\'s potential cannot replace a hard decision based on the numbers.</p>\n<ul><li><strong>Ship:</strong> You ship a candidate only when the primary score clearly beats the frozen baseline, the regression is within acceptable limits, the failure classes are well-understood, and the entire artifact can be reproduced.</li>\n<li><strong>Retry:</strong> You decide to retry (e.g., retry-data or retry-training) when the model shows promise but fails a critical constraint. For instance, a DPO run that improves execution scores but fails to fix output-format hygiene requires a data retry, not a ship.</li>\n<li><strong>Reject:</strong> You reject a candidate when the primary score does not beat the baseline, when breadth damage erases the gain, or when the evaluation itself is deemed too unstable to trust. Rejecting a model is a successful application of the evaluation protocol.</li></ul>\n<p>By strictly enforcing these decisions, you ensure that every fine-tuning run either improves the system or provides a documented learning outcome, avoiding the accumulation of untrusted or marginally useful models.</p>\n<h2>Summary of the Protocol</h2>\n<p>To effectively compare two fine-tuning runs without moving the evaluation target, adhere to these core principles:</p>\n<ol><li><strong>Freeze the Eval</strong>: Define your dataset, your parser, and your baseline score <em>before</em> any training code is executed.</li>\n<li><strong>Calibrate with Frontier</strong>: Ensure a frontier model can achieve near 100% on your benchmark. If it cannot, you are optimizing against noise.</li>\n<li><strong>Measure the Trade-off</strong>: Always report both the primary task score (depth) and a regression/breadth score to accurately capture catastrophic forgetting.</li>\n<li><strong>Use Semantic Matching</strong>: Avoid brittle exact-string match; employ AST parsing or semantic evaluation to measure true capability.</li>\n<li><strong>Force a Decision</strong>: Use the evaluation data to make a clear Ship, Retry, or Reject decision, recording all metrics in a durable report.</li></ol>\n<p>By adopting this evidence-backed protocol, you can trust your evaluation metrics and make confident decisions about the value of your fine-tuning experiments.</p>\n<h2>Next Action</h2>\n<p>To apply these principles today, start by defining a completely frozen baseline for your current project. Select a small, groundable dataset tailored to your primary task. Run a frontier model against this dataset to confirm a &gt;95% ceiling. Then, evaluate your stock base model to establish your starting score. Only after these numbers are recorded should you begin your next fine-tuning experiment.</p>',
    },
  "/articles/lora-evaluation-workflow-mac-local-models": {
    title: "A Practical LoRA Evaluation Workflow for Mac-Local Models",
    description:
      "Learn how to evaluate Mac-local LoRA adapters using frozen baselines, frontier calibration, AST matching, regression gates, and automated report cards.",
    path: "/articles/lora-evaluation-workflow-mac-local-models",
    intent:
      "Informational / Practical Developer Guide. AI developers and engineers fine-tuning local models on macOS seeking a rigorous evaluation harness, benchmark calibration, regression gates, and report card protocols for LoRA adapters.",
    schema: ["HowTo", "BreadcrumbList"],
    internalLinks: [
      "/articles",
      "/evaluate-local-llm",
      "/articles/how-to-compare-two-fine-tuning-runs-without-moving-the-evaluation-target",
      "/fine-tune-llm-on-mac",
      "/artifacts",
      "/leaderboard",
    ],
    markdown:
      '## Introduction: The Evaluation Deficit in Local Fine-Tuning\n\nIn local machine learning development on Apple Silicon, training a Low-Rank Adaptation (LoRA) adapter is often the easiest part of the pipeline. Tools like MLX and PyTorch MPS make it straightforward to run Supervised Fine-Tuning (SFT) or Direct Preference Optimization (DPO) on small base models. The true bottleneck is evaluation: knowing whether a fine-tuned adapter actually improved task capability, created subtle regressions, or simply memorized the training set.\n\nRelying on training loss curves is a common pitfall. A LoRA adapter can achieve a low training loss (e.g., 0.001) while completely collapsing on out-of-domain tasks, unhandled edge cases, or format constraints. A practical evaluation workflow for Mac-local models requires frozen baselines, frontier-calibrated benchmark gates, automated regression checks, and explicit decision boundaries.\n\nWhen structuring evaluation documentation, it is critical to maintain four distinct levels of detail:\n- **General Guidance:** Conceptual principles of local model evaluation.\n- **Recipe Contracts:** Frozen evaluation parameters, dataset versions, and decision thresholds.\n- **Public Artifacts:** Verifiable report cards and benchmark trace JSONs.\n- **Actual Runs:** Empirical outcomes, score deltas, and failure dispositions from real local evaluations.\n\n## Evaluation Philosophy: Frontier Models as Calibration Anchors\n\nAn evaluation metric is only as good as the ruler used to measure it. When evaluating small Mac-local models (0.6B to 4B parameters), frontier models (such as Claude 3.5 Sonnet or GPT-4o / Codex) serve as the **calibration anchor** for every benchmark.\n\n### The Frontier-Ceiling Gate\nBefore any benchmark is used to grade local Mac models, a top-tier frontier model must score approximately **~100%** on it. If a frontier model cannot ace a benchmark, the benchmark itself is broken—containing ungroundable golds, ambiguous prompts, or broken exact-match constraints.\n\n**Rule:** Never report local model accuracy on a benchmark that fails the frontier-ceiling gate.\n\n```text\n[ Proposed Benchmark ] ──> [ Run Frontier Model ] ──> Score < 95%? ──> [ FIX or DROP Benchmark ]\n                                                 │\n                                                 └── Score ~100% ──> [ APPROVED Benchmark Gate ]\n```\n\n### Groundable AST Matching vs. Exact-String Traps\nA concrete example of benchmark failure is the `hermes-fc` tool-calling dataset. When tested against frontier models, exact-match scoring produced a ~12% ceiling because ~29% of target gold answers contained ungroundable placeables (e.g., random transaction IDs, synthetic device IDs, or literal `"unique_nft_identifier"` placeholders missing from the prompt context). Frontier models generated valid, contextually grounded answers that were penalized by exact string matching. Consequently, `hermes-fc` was reclassified as training-only data and rejected as an evaluation metric.\n\nIn contrast, benchmarks like the Berkeley Function Calling Leaderboard (BFCL) rely on Abstract Syntax Tree (AST) matching, semantic argument verification, and groundable golds. Frontier models reach ~99% accuracy on BFCL multi-turn slices, making it a valid, calibrated gate for local model evaluation.\n\n## The Two-Dimensional Evaluation Framework: Depth vs. Breadth\n\nA single accuracy score is insufficient to evaluate a LoRA adapter. Small language models operating under tight parameter constraints frequently suffer from negative transfer or catastrophic forgetting. Evaluation must track two distinct dimensions:\n\n```text\n               ┌─────────────────────────────────────────────────┐\n               │              Targeted Task Depth                │\n               │   (e.g., File-Ops / SQL Execution Accuracy)    │\n               └────────────────────────┬────────────────────────┘\n                                        │\n                                        ▼\n               ┌─────────────────────────────────────────────────┐\n               │           Out-of-Domain Breadth Gate            │\n               │    (e.g., General Planning / Reason Slices)     │\n               └─────────────────────────────────────────────────┘\n```\n\n1. **Targeted Task Depth:** Measures specific performance gains in the target domain (e.g., executing file management tool calls or generating executable SQLite queries).\n2. **Out-of-Domain Breadth:** Measures potential degradation across general capabilities (e.g., multi-turn conversational planning or general reasoning).\n\nAn adapter that achieves 100% depth but degrades out-of-domain breadth by 20% cannot be released as a general model replacement; it must either be rejected or deployed as a **routed specialist**.\n\n## Step-by-Step LoRA Evaluation Workflow\n\nA reproducible evaluation pipeline on macOS follows four automated steps:\n\n```text\n[ 1. Re-Stamp Baseline ] ──> [ 2. Execute Eval Gates ] ──> [ 3. Extract Slices ] ──> [ 4. Build Report Card ]\n```\n\n### Step 1: Baseline Re-Stamping and Environment Pinning\nBefore running evaluation on a newly trained adapter, re-evaluate the stock base model on the exact same hardware and harness version. Record the exact commit hash, MLX/PyTorch runtime version, seed, and temperature parameters.\n\n### Step 2: Executing Deterministic Evaluation Gates\nRun target-specific evaluation scripts using native Mac tools:\n- **`posttrainllm eval-gate`:** Executes standardized multi-turn tool-calling and planning suites.\n- **`posttrainllm eval-sql`:** Executes generated SQL queries against isolated SQLite database instances (`.sqlite` files) to measure true execution accuracy rather than string exact-match.\n\n### Step 3: Extracting Slice Metrics and Trace Reviews\nGenerate granular slice metrics to identify where errors occur. For example, in SQL evaluation, categorize performance into single-table queries, multi-table joins, `GROUP BY` aggregations, and output format hygiene (detecting unwanted prose wrappers around raw SQL).\n\n### Step 4: Compiling Immutable Fine-Tune Report Cards\nCompile all evaluation artifacts into a versioned, portable JSON record (`report-card.json`) and static HTML summary page using automated scripts (`scripts/factory/build_fine_tune_report_card.py`).\n\nEvery field in the report card carries an explicit measurement state (`measured`, `derived`, `historical`, or `skipped`).\n\n## Real Local Case Studies: Evaluation Dispositions\n\nReviewing real evaluation records from the posttrainllm repository illustrates how this workflow governs model deployment:\n\n### Case Study 1: `qwen3-4b-file-ops-distilled`\n- **Base Model:** Qwen3-4B-Instruct.\n- **Depth Result:** File-operations hard gate improved from 58.0% (stock) to **100.0%**.\n- **Breadth Result:** Out-of-domain general planning breadth regressed from 59.6% down to **42.3%**.\n- **Evaluation Disposition:** `routed-ship`. The adapter was packaged strictly for routed deployment behind an intent classifier; general replacement was blocked.\n\n### Case Study 2: `qwen3-4b-rest-fused`\n- **Base Model:** Qwen3-4B-Instruct + ReST synthetic fusion.\n- **Depth Result:** Re-qualification confirmed file-ops depth moved from 9/12 to **12/12** (100%), with side-effect errors reduced and execution speed increasing by 2.42×.\n- **Breadth Result:** Frontier-qualified general breadth dropped from 30/45 down to **25/45**.\n- **Evaluation Disposition:** `routed-ship` retained as a routed specialist; rejected as a general model successor.\n\n### Case Study 3: `qwen06-sql-hygiene-dpo-v1`\n- **Base Model:** Qwen3-0.6B + SFT DoRA adapter.\n- **SimPO Experiment:** Reference-free SimPO preference loss caused policy collapse (execution dropped from 86.0% to 8.0%).\n- **DPO Ref-Anchored Retry:** Reference-anchored DPO ($\\beta=0.3$) recovered and improved execution accuracy to **92.0%**, but failed the raw format hygiene requirement (`clean-SQL` rate remained 0.000).\n- **Evaluation Disposition:** `retry-data`. The evaluation proved that format hygiene on this base model required SFT generation-strength steering or constrained decoding rather than preference updates.\n\n## Decision Authority and Release Governance\n\nAn evaluation workflow must culminate in an unambiguous, terminal decision. The posttrainllm framework recognizes four formal disposition categories:\n\n| Decision | Criteria & Conditions | Next Action |\n| :--- | :--- | :--- |\n| **`ship`** | Passes target depth gate AND shows zero significant breadth regression. | Package as general model replacement. |\n| **`routed-ship`** | Reaches target depth gate BUT exhibits out-of-domain breadth regression. | Package strictly behind domain-specific router. |\n| **`retry-data`** | Fails target depth or hygiene gate due to data/prompt formatting limitations. | Re-design training data or SFT recipe; do not adjust eval gates. |\n| **`reject`** | Fails safety, accuracy, or capacity constraints without clear recovery path. | Archive experiment as failed attempt; release no artifact. |\n\n## Clear Next Action\n\nTo implement this evaluation workflow for your Mac-local LoRA adapters:\n1. Establish a frozen baseline evaluation run for your base model using `posttrainllm eval-gate --save-baseline`.\n2. Validate your evaluation benchmark against a frontier API (or Codex CLI) to confirm a ~100% ceiling.\n3. Train your LoRA adapter and run `posttrainllm eval-gate` alongside your out-of-domain regression suite.\n4. Execute `python3 scripts/factory/build_fine_tune_report_card.py --run-dir <path>` to generate a `report-card.json`.\n5. Assign a formal terminal decision (`ship`, `routed-ship`, `retry-data`, or `reject`) based on the depth vs. breadth outcome.',
    html: "<h2>Introduction: The Evaluation Deficit in Local Fine-Tuning</h2>\n<p>In local machine learning development on Apple Silicon, training a Low-Rank Adaptation (LoRA) adapter is often the easiest part of the pipeline. Tools like MLX and PyTorch MPS make it straightforward to run Supervised Fine-Tuning (SFT) or Direct Preference Optimization (DPO) on small base models. The true bottleneck is evaluation: knowing whether a fine-tuned adapter actually improved task capability, created subtle regressions, or simply memorized the training set.</p>\n<p>Relying on training loss curves is a common pitfall. A LoRA adapter can achieve a low training loss (e.g., 0.001) while completely collapsing on out-of-domain tasks, unhandled edge cases, or format constraints. A practical evaluation workflow for Mac-local models requires frozen baselines, frontier-calibrated benchmark gates, automated regression checks, and explicit decision boundaries.</p>\n<p>When structuring evaluation documentation, it is critical to maintain four distinct levels of detail:</p>\n<ul><li><strong>General Guidance:</strong> Conceptual principles of local model evaluation.</li>\n<li><strong>Recipe Contracts:</strong> Frozen evaluation parameters, dataset versions, and decision thresholds.</li>\n<li><strong>Public Artifacts:</strong> Verifiable report cards and benchmark trace JSONs.</li>\n<li><strong>Actual Runs:</strong> Empirical outcomes, score deltas, and failure dispositions from real local evaluations.</li></ul>\n<h2>Evaluation Philosophy: Frontier Models as Calibration Anchors</h2>\n<p>An evaluation metric is only as good as the ruler used to measure it. When evaluating small Mac-local models (0.6B to 4B parameters), frontier models (such as Claude 3.5 Sonnet or GPT-4o / Codex) serve as the <strong>calibration anchor</strong> for every benchmark.</p>\n<h3>The Frontier-Ceiling Gate</h3>\n<p>Before any benchmark is used to grade local Mac models, a top-tier frontier model must score approximately <strong>~100%</strong> on it. If a frontier model cannot ace a benchmark, the benchmark itself is broken—containing ungroundable golds, ambiguous prompts, or broken exact-match constraints.</p>\n<p><strong>Rule:</strong> Never report local model accuracy on a benchmark that fails the frontier-ceiling gate.</p>\n<pre><code>[ Proposed Benchmark ] ──&gt; [ Run Frontier Model ] ──&gt; Score &lt; 95%? ──&gt; [ FIX or DROP Benchmark ]\n                                                 │\n                                                 └── Score ~100% ──&gt; [ APPROVED Benchmark Gate ]</code></pre>\n<h3>Groundable AST Matching vs. Exact-String Traps</h3>\n<p>A concrete example of benchmark failure is the <code>hermes-fc</code> tool-calling dataset. When tested against frontier models, exact-match scoring produced a ~12% ceiling because ~29% of target gold answers contained ungroundable placeables (e.g., random transaction IDs, synthetic device IDs, or literal <code>&quot;unique_nft_identifier&quot;</code> placeholders missing from the prompt context). Frontier models generated valid, contextually grounded answers that were penalized by exact string matching. Consequently, <code>hermes-fc</code> was reclassified as training-only data and rejected as an evaluation metric.</p>\n<p>In contrast, benchmarks like the Berkeley Function Calling Leaderboard (BFCL) rely on Abstract Syntax Tree (AST) matching, semantic argument verification, and groundable golds. Frontier models reach ~99% accuracy on BFCL multi-turn slices, making it a valid, calibrated gate for local model evaluation.</p>\n<h2>The Two-Dimensional Evaluation Framework: Depth vs. Breadth</h2>\n<p>A single accuracy score is insufficient to evaluate a LoRA adapter. Small language models operating under tight parameter constraints frequently suffer from negative transfer or catastrophic forgetting. Evaluation must track two distinct dimensions:</p>\n<pre><code>               ┌─────────────────────────────────────────────────┐\n               │              Targeted Task Depth                │\n               │   (e.g., File-Ops / SQL Execution Accuracy)    │\n               └────────────────────────┬────────────────────────┘\n                                        │\n                                        ▼\n               ┌─────────────────────────────────────────────────┐\n               │           Out-of-Domain Breadth Gate            │\n               │    (e.g., General Planning / Reason Slices)     │\n               └─────────────────────────────────────────────────┘</code></pre>\n<ol><li><strong>Targeted Task Depth:</strong> Measures specific performance gains in the target domain (e.g., executing file management tool calls or generating executable SQLite queries).</li>\n<li><strong>Out-of-Domain Breadth:</strong> Measures potential degradation across general capabilities (e.g., multi-turn conversational planning or general reasoning).</li></ol>\n<p>An adapter that achieves 100% depth but degrades out-of-domain breadth by 20% cannot be released as a general model replacement; it must either be rejected or deployed as a <strong>routed specialist</strong>.</p>\n<h2>Step-by-Step LoRA Evaluation Workflow</h2>\n<p>A reproducible evaluation pipeline on macOS follows four automated steps:</p>\n<pre><code>[ 1. Re-Stamp Baseline ] ──&gt; [ 2. Execute Eval Gates ] ──&gt; [ 3. Extract Slices ] ──&gt; [ 4. Build Report Card ]</code></pre>\n<h3>Step 1: Baseline Re-Stamping and Environment Pinning</h3>\n<p>Before running evaluation on a newly trained adapter, re-evaluate the stock base model on the exact same hardware and harness version. Record the exact commit hash, MLX/PyTorch runtime version, seed, and temperature parameters.</p>\n<h3>Step 2: Executing Deterministic Evaluation Gates</h3>\n<p>Run target-specific evaluation scripts using native Mac tools:</p>\n<ul><li><strong><code>posttrainllm eval-gate</code>:</strong> Executes standardized multi-turn tool-calling and planning suites.</li>\n<li><strong><code>posttrainllm eval-sql</code>:</strong> Executes generated SQL queries against isolated SQLite database instances (<code>.sqlite</code> files) to measure true execution accuracy rather than string exact-match.</li></ul>\n<h3>Step 3: Extracting Slice Metrics and Trace Reviews</h3>\n<p>Generate granular slice metrics to identify where errors occur. For example, in SQL evaluation, categorize performance into single-table queries, multi-table joins, <code>GROUP BY</code> aggregations, and output format hygiene (detecting unwanted prose wrappers around raw SQL).</p>\n<h3>Step 4: Compiling Immutable Fine-Tune Report Cards</h3>\n<p>Compile all evaluation artifacts into a versioned, portable JSON record (<code>report-card.json</code>) and static HTML summary page using automated scripts (<code>scripts/factory/build_fine_tune_report_card.py</code>).</p>\n<p>Every field in the report card carries an explicit measurement state (<code>measured</code>, <code>derived</code>, <code>historical</code>, or <code>skipped</code>).</p>\n<h2>Real Local Case Studies: Evaluation Dispositions</h2>\n<p>Reviewing real evaluation records from the posttrainllm repository illustrates how this workflow governs model deployment:</p>\n<h3>Case Study 1: <code>qwen3-4b-file-ops-distilled</code></h3>\n<ul><li><strong>Base Model:</strong> Qwen3-4B-Instruct.</li>\n<li><strong>Depth Result:</strong> File-operations hard gate improved from 58.0% (stock) to <strong>100.0%</strong>.</li>\n<li><strong>Breadth Result:</strong> Out-of-domain general planning breadth regressed from 59.6% down to <strong>42.3%</strong>.</li>\n<li><strong>Evaluation Disposition:</strong> <code>routed-ship</code>. The adapter was packaged strictly for routed deployment behind an intent classifier; general replacement was blocked.</li></ul>\n<h3>Case Study 2: <code>qwen3-4b-rest-fused</code></h3>\n<ul><li><strong>Base Model:</strong> Qwen3-4B-Instruct + ReST synthetic fusion.</li>\n<li><strong>Depth Result:</strong> Re-qualification confirmed file-ops depth moved from 9/12 to <strong>12/12</strong> (100%), with side-effect errors reduced and execution speed increasing by 2.42×.</li>\n<li><strong>Breadth Result:</strong> Frontier-qualified general breadth dropped from 30/45 down to <strong>25/45</strong>.</li>\n<li><strong>Evaluation Disposition:</strong> <code>routed-ship</code> retained as a routed specialist; rejected as a general model successor.</li></ul>\n<h3>Case Study 3: <code>qwen06-sql-hygiene-dpo-v1</code></h3>\n<ul><li><strong>Base Model:</strong> Qwen3-0.6B + SFT DoRA adapter.</li>\n<li><strong>SimPO Experiment:</strong> Reference-free SimPO preference loss caused policy collapse (execution dropped from 86.0% to 8.0%).</li>\n<li><strong>DPO Ref-Anchored Retry:</strong> Reference-anchored DPO (<code>β=0.3</code>) recovered and improved execution accuracy to <strong>92.0%</strong>, but failed the raw format hygiene requirement (<code>clean-SQL</code> rate remained 0.000).</li>\n<li><strong>Evaluation Disposition:</strong> <code>retry-data</code>. The evaluation proved that format hygiene on this base model required SFT generation-strength steering or constrained decoding rather than preference updates.</li></ul>\n<h2>Decision Authority and Release Governance</h2>\n<p>An evaluation workflow must culminate in an unambiguous, terminal decision. The posttrainllm framework recognizes four formal disposition categories:</p>\n<table><thead><tr><th>Decision</th><th>Criteria &amp; Conditions</th><th>Next Action</th></tr></thead><tbody><tr><td><strong><code>ship</code></strong></td><td>Passes target depth gate AND shows zero significant breadth regression.</td><td>Package as general model replacement.</td></tr><tr><td><strong><code>routed-ship</code></strong></td><td>Reaches target depth gate BUT exhibits out-of-domain breadth regression.</td><td>Package strictly behind domain-specific router.</td></tr><tr><td><strong><code>retry-data</code></strong></td><td>Fails target depth or hygiene gate due to data/prompt formatting limitations.</td><td>Re-design training data or SFT recipe; do not adjust eval gates.</td></tr><tr><td><strong><code>reject</code></strong></td><td>Fails safety, accuracy, or capacity constraints without clear recovery path.</td><td>Archive experiment as failed attempt; release no artifact.</td></tr></tbody></table>\n<h2>Clear Next Action</h2>\n<p>To implement this evaluation workflow for your Mac-local LoRA adapters:</p>\n<ol><li>Establish a frozen baseline evaluation run for your base model using <code>posttrainllm eval-gate --save-baseline</code>.</li>\n<li>Validate your evaluation benchmark against a frontier API (or Codex CLI) to confirm a ~100% ceiling.</li>\n<li>Train your LoRA adapter and run <code>posttrainllm eval-gate</code> alongside your out-of-domain regression suite.</li>\n<li>Execute <code>python3 scripts/factory/build_fine_tune_report_card.py --run-dir &lt;path&gt;</code> to generate a <code>report-card.json</code>.</li>\n<li>Assign a formal terminal decision (<code>ship</code>, <code>routed-ship</code>, <code>retry-data</code>, or <code>reject</code>) based on the depth vs. breadth outcome.</li></ol>",
  },
  "/articles/post-train-small-language-model-apple-silicon": {
    title: "How to Post-Train a Small Language Model on Apple Silicon",
    description:
      "Learn how to post-train 0.6B–4B language models locally on Apple Silicon using LoRA, MPS, and MLX. Includes real run data, factory loops, and hardware constraints.",
    path: "/articles/post-train-small-language-model-apple-silicon",
    intent:
      "Informational / Practical Developer Guide. Developers and ML practitioners seeking to adapt and fine-tune small open models (0.6B to 4B parameters) locally on Apple Silicon Macs using native frameworks like MLX and PyTorch (MPS) without cloud GPUs.",
    schema: ["HowTo", "BreadcrumbList"],
    internalLinks: [
      "/articles",
      "/how-to-train-your-own-llm",
      "/fine-tune-llm-on-mac",
      "/mlx-lora-fine-tuning",
      "/docs/quickstart",
      "/recipes",
    ],
    markdown:
      "## Introduction: Local Post-Training on Apple Silicon\n\nAdapting small language models (0.6B to 4B parameters) on local hardware has transitioned from a novelty to a practical engineering discipline. Apple Silicon's Unified Memory Architecture (UMA) gives a single workstation high-bandwidth shared memory across CPU and GPU cores, enabling local execution of training, preference alignment, and evaluation loops that previously required cloud GPU instances.\n\nHowever, training on a Mac requires a shift in mindset. You are not competing with multi-node clusters or frontier-scale pre-training. Instead, the goal is domain specialization: taking a general small base model—such as Qwen3-0.6B, Qwen3-4B, or FLAN-T5-small—and post-training it to reliably execute structured tasks, tool calls, or domain-specific queries within strict RAM and latency budgets.\n\nUnderstanding local post-training requires distinguishing four distinct categories of information:\n- **General Guidance:** Architectural principles and workflow patterns.\n- **Recipe Contracts:** Fixed, versioned parameters (learning rate, rank, batch size, stop rules).\n- **Public Artifacts:** Packaged, inspectable weights and metadata output from a run.\n- **Actual Runs:** Empirical benchmark results and failure analysis from real local executions.\n\n## The Local Post-Training Factory Loop\n\nA structured post-training workflow relies on a closed evidence loop rather than ad-hoc parameter tweaks. Every local experiment follows a deterministic sequence:\n\n```text\ntarget -> data -> post-training -> eval -> package -> report\n```\n\n1. **Target:** Define a single task or domain (e.g., SQL generation or file-system action grounding) alongside explicit pass/fail criteria before touching weights.\n2. **Data:** Prepare cleaned train and held-out evaluation splits. Data cleanliness and format alignment take precedence over raw token volume.\n3. **Post-Training:** Apply Supervised Fine-Tuning (SFT), Direct Preference Optimization (DPO), or Distillation using a parameter-efficient adapter recipe.\n4. **Eval:** Measure candidate performance against a frozen baseline across both targeted depth and out-of-domain regression suites.\n5. **Package:** Export weights into standardized formats (MLX adapters, safetensors, or GGUF) with embedded provenance metadata.\n6. **Report:** Generate an immutable before-and-after report card documenting score delta, latency, peak memory (Peak RSS), and the terminal decision (`ship`, `routed-ship`, `retry-data`, or `reject`).\n\n## Execution Runtimes: Swift/MLX vs. Python/PyTorch (MPS)\n\nOn macOS, developers typically choose between two execution environments for post-training:\n\n### 1. Native Swift/MLX Runtime\nThe MLX framework, integrated into native Swift command-line tools (`posttrainllm`), offers direct interaction with Metal performance primitives. Swift/MLX runtimes deliver low cold-start latency and efficient memory reuse during low-rank adaptation. Commands like `posttrainllm sft` or `posttrainllm dpo` operate natively against Apple Silicon GPU pipelines without Python process overhead.\n\n### 2. Python / PyTorch (MPS) Reference Paths\nFor custom architectures or research experiments (such as encoder-decoder models), PyTorch with the Metal Performance Shaders (`mps`) backend provides flexibility. When hand-rolling Low-Rank Adaptation (LoRA) modules—such as injecting adapter matrices $A$ and $B$ into attention projection layers ($Q, K, V$)—using PyTorch MPS enables step-by-step gradient inspection and loss tracing.\n\n### Host Protection & Resource Management\nHeavy GPU compute loops on macOS can cause WindowServer responsiveness issues if unconstrained. Local post-training workflows must use process lockfiles (e.g., `~/.cache/posttrainllm/gpu.lock`) to prevent concurrent GPU workloads from colliding, while setting cooperative batch sizes to maintain system stability.\n\n## Post-Training Techniques on Mac Hardware\n\n### Parameter-Efficient Adaptation (LoRA & DoRA)\nFull parameter fine-tuning of even a 4B model on local hardware consumes significant memory due to optimizer state overhead (AdamW requires 8 bytes per parameter for momentum and variance buffers alone). Parameter-Efficient Fine-Tuning (PEFT) techniques like Low-Rank Adaptation (LoRA) and Weight-Decomposed Low-Rank Adaptation (DoRA) restrict trainable parameters to low-rank decomposition matrices ($r=4$ or $r=8$) injected into key projections (`q_proj`, `v_proj`). On a 0.6B parameter base model, a rank-4 LoRA adapter adds fewer than 500,000 trainable parameters (<0.1% of base size), keeping peak memory usage well under 2 GB during training.\n\n### Preference Alignment: DPO vs. SimPO\nWhen refining model formatting or output tone, Direct Preference Optimization (DPO) aligns model generation against preferred and dispreferred pairs without training a separate reward model.\n\nLocal experiments highlight a critical stability distinction between preference loss formulations:\n- **Reference-Free Loss (SimPO):** Removing the reference model anchor during preference updates can cause severe policy collapse on small models. In local SQL hygiene experiments on Qwen3-0.6B, reference-free SimPO caused execution accuracy to plummet from 86.0% to 8.0%, resulting in repetitive fence and prose outputs.\n- **Reference-Anchored DPO:** Maintaining a frozen reference model anchor ($\\beta=0.1$ to $0.3$) stabilizes updates. On the same Qwen3-0.6B target, reference-anchored DPO preserved and enhanced execution accuracy (rising from 86.0% to 92.0%).\n\n### Knowledge Distillation\nFor complex reasoning or multi-turn tool execution, direct SFT on small models often hits capacity limits. Distillation—training a small student model on high-quality action trajectories generated by larger teacher models (or frontier APIs)—enables small models to learn structured output conventions that would otherwise require multi-billion parameter capacity.\n\n## Real Local Runs, Artifacts, and Lessons Learned\n\nLocal post-training produces concrete empirical evidence. Reviewing actual runs recorded in posttrainllm reveals key lessons:\n\n### Case Study 1: `qwen3-4b-file-ops-distilled` (Routed Specialist Win)\n- **Objective:** Improve file-operation tool-calling accuracy on a 4B parameter model.\n- **Result:** Distillation improved file-operations hard gate task completion from 58.0% to 100.0%.\n- **Regression Check:** Out-of-domain general planning breadth regressed from 59.6% down to 42.3%.\n- **Decision:** Released as a `routed-ship` specialist artifact. The model is deployed exclusively behind a file-ops route; it is explicitly rejected as a general planner replacement.\n\n### Case Study 2: `qwen06-sql-hygiene-dpo-v1` (Format Prior Limitations)\n- **Objective:** Suppress unwanted prose wrappers (e.g., `Answer: ...`) around SQL SELECT statements on Qwen3-0.6B.\n- **Result:** Higher-pressure reference-anchored DPO ($\\beta=0.3$, 200 steps) successfully minimized preference loss ($0.6931 \\to 0.0073$) and boosted inner SQL execution from 86.0% to 92.0%. However, greedy generation still retained the prose wrapper (`clean-SQL` raw rate remained at 0.000).\n- **Decision:** `retry-data`. Diagnosis revealed that the base model's strong prose prior could not be erased by rank-4 preference adapters alone when SFT targets were already clean. Format hygiene required generation-time constrained sampling or stronger initial SFT rather than preference updates.\n\n### Case Study 3: FLAN-T5-Small Autocorrect Pilot (Overcorrection Failure)\n- **Objective:** Train an encoder-decoder model (FLAN-T5-small, 60M parameters) for text error correction using hand-rolled PyTorch MPS LoRA.\n- **Result:** Ordinary sequence loss over 50 steps caused negative error reduction (+0.0625 to -0.8125). The adapter transformed from a subtle error corrector into an aggressive paraphraser (e.g., changing `remeber` to `remind you` rather than `remember`).\n- **Lesson:** Sequence loss alone on thin datasets fails to enforce minimum-edit distance constraints.\n\n## Hardware Constraints and Performance Realities\n\nWhen post-training on Apple Silicon, hardware metrics directly govern viability:\n\n| Parameter Count | Quantization / Format | Peak Training RSS | Decode Speed (M5 Pro / M3) | Typical Use Case |\n| :--- | :--- | :--- | :--- | :--- |\n| **0.6B** | BF16 / Float32 | ~1.1 GB – 1.8 GB | ~120 – 180 tok/s | Fast local router, bare SQL, single-intent classifiers |\n| **1.7B** | BF16 / Q4_K_M | ~2.5 GB – 4.0 GB | ~60 – 90 tok/s | Single-turn tool calling, structured JSON extraction |\n| **4.0B** | BF16 / Q4_0 | ~5.5 GB – 9.0 GB | ~30 – 50 tok/s | Multi-turn agentic planning, routed specialists |\n\nKey hardware lessons:\n1. **Low Loss $\\neq$ High Capability:** Training loss reaching near-zero (e.g., 0.001) often indicates memorization rather than generalization.\n2. **Unified Memory Headroom:** Always reserve at least 20–30% of system RAM for macOS WindowServer and buffer caches to prevent thermal throttling or page swapping.\n\n## Clear Next Action\n\nTo run your own local post-training experiment on Apple Silicon:\n1. Clone the repository and build the native CLI using `swift build -c release`.\n2. Prepare a paired dataset following the run schema contract in `docs/factory/run-schema.md`.\n3. Freeze your evaluation baseline before initiating training.\n4. Execute `posttrainllm sft` with a low-rank adapter config ($r=4$ or $r=8$).\n5. Run `posttrainllm eval-gate` to generate an immutable report card before deciding whether to ship or retry.",
    html: "<h2>Introduction: Local Post-Training on Apple Silicon</h2>\n<p>Adapting small language models (0.6B to 4B parameters) on local hardware has transitioned from a novelty to a practical engineering discipline. Apple Silicon's Unified Memory Architecture (UMA) gives a single workstation high-bandwidth shared memory across CPU and GPU cores, enabling local execution of training, preference alignment, and evaluation loops that previously required cloud GPU instances.</p>\n<p>However, training on a Mac requires a shift in mindset. You are not competing with multi-node clusters or frontier-scale pre-training. Instead, the goal is domain specialization: taking a general small base model—such as Qwen3-0.6B, Qwen3-4B, or FLAN-T5-small—and post-training it to reliably execute structured tasks, tool calls, or domain-specific queries within strict RAM and latency budgets.</p>\n<p>Understanding local post-training requires distinguishing four distinct categories of information:</p>\n<ul><li><strong>General Guidance:</strong> Architectural principles and workflow patterns.</li>\n<li><strong>Recipe Contracts:</strong> Fixed, versioned parameters (learning rate, rank, batch size, stop rules).</li>\n<li><strong>Public Artifacts:</strong> Packaged, inspectable weights and metadata output from a run.</li>\n<li><strong>Actual Runs:</strong> Empirical benchmark results and failure analysis from real local executions.</li></ul>\n<h2>The Local Post-Training Factory Loop</h2>\n<p>A structured post-training workflow relies on a closed evidence loop rather than ad-hoc parameter tweaks. Every local experiment follows a deterministic sequence:</p>\n<pre><code>target -&gt; data -&gt; post-training -&gt; eval -&gt; package -&gt; report</code></pre>\n<ol><li><strong>Target:</strong> Define a single task or domain (e.g., SQL generation or file-system action grounding) alongside explicit pass/fail criteria before touching weights.</li>\n<li><strong>Data:</strong> Prepare cleaned train and held-out evaluation splits. Data cleanliness and format alignment take precedence over raw token volume.</li>\n<li><strong>Post-Training:</strong> Apply Supervised Fine-Tuning (SFT), Direct Preference Optimization (DPO), or Distillation using a parameter-efficient adapter recipe.</li>\n<li><strong>Eval:</strong> Measure candidate performance against a frozen baseline across both targeted depth and out-of-domain regression suites.</li>\n<li><strong>Package:</strong> Export weights into standardized formats (MLX adapters, safetensors, or GGUF) with embedded provenance metadata.</li>\n<li><strong>Report:</strong> Generate an immutable before-and-after report card documenting score delta, latency, peak memory (Peak RSS), and the terminal decision (<code>ship</code>, <code>routed-ship</code>, <code>retry-data</code>, or <code>reject</code>).</li></ol>\n<h2>Execution Runtimes: Swift/MLX vs. Python/PyTorch (MPS)</h2>\n<p>On macOS, developers typically choose between two execution environments for post-training:</p>\n<h3>1. Native Swift/MLX Runtime</h3>\n<p>The MLX framework, integrated into native Swift command-line tools (<code>posttrainllm</code>), offers direct interaction with Metal performance primitives. Swift/MLX runtimes deliver low cold-start latency and efficient memory reuse during low-rank adaptation. Commands like <code>posttrainllm sft</code> or <code>posttrainllm dpo</code> operate natively against Apple Silicon GPU pipelines without Python process overhead.</p>\n<h3>2. Python / PyTorch (MPS) Reference Paths</h3>\n<p>For custom architectures or research experiments (such as encoder-decoder models), PyTorch with the Metal Performance Shaders (<code>mps</code>) backend provides flexibility. When hand-rolling Low-Rank Adaptation (LoRA) modules—such as injecting adapter matrices <code>A</code> and <code>B</code> into attention projection layers (<code>Q, K, V</code>)—using PyTorch MPS enables step-by-step gradient inspection and loss tracing.</p>\n<h3>Host Protection &amp; Resource Management</h3>\n<p>Heavy GPU compute loops on macOS can cause WindowServer responsiveness issues if unconstrained. Local post-training workflows must use process lockfiles (e.g., <code>~/.cache/posttrainllm/gpu.lock</code>) to prevent concurrent GPU workloads from colliding, while setting cooperative batch sizes to maintain system stability.</p>\n<h2>Post-Training Techniques on Mac Hardware</h2>\n<h3>Parameter-Efficient Adaptation (LoRA &amp; DoRA)</h3>\n<p>Full parameter fine-tuning of even a 4B model on local hardware consumes significant memory due to optimizer state overhead (AdamW requires 8 bytes per parameter for momentum and variance buffers alone). Parameter-Efficient Fine-Tuning (PEFT) techniques like Low-Rank Adaptation (LoRA) and Weight-Decomposed Low-Rank Adaptation (DoRA) restrict trainable parameters to low-rank decomposition matrices (<code>r=4</code> or <code>r=8</code>) injected into key projections (<code>q_proj</code>, <code>v_proj</code>). On a 0.6B parameter base model, a rank-4 LoRA adapter adds fewer than 500,000 trainable parameters (&lt;0.1% of base size), keeping peak memory usage well under 2 GB during training.</p>\n<h3>Preference Alignment: DPO vs. SimPO</h3>\n<p>When refining model formatting or output tone, Direct Preference Optimization (DPO) aligns model generation against preferred and dispreferred pairs without training a separate reward model.</p>\n<p>Local experiments highlight a critical stability distinction between preference loss formulations:</p>\n<ul><li><strong>Reference-Free Loss (SimPO):</strong> Removing the reference model anchor during preference updates can cause severe policy collapse on small models. In local SQL hygiene experiments on Qwen3-0.6B, reference-free SimPO caused execution accuracy to plummet from 86.0% to 8.0%, resulting in repetitive fence and prose outputs.</li>\n<li><strong>Reference-Anchored DPO:</strong> Maintaining a frozen reference model anchor (<code>β=0.1</code> to <code>0.3</code>) stabilizes updates. On the same Qwen3-0.6B target, reference-anchored DPO preserved and enhanced execution accuracy (rising from 86.0% to 92.0%).</li></ul>\n<h3>Knowledge Distillation</h3>\n<p>For complex reasoning or multi-turn tool execution, direct SFT on small models often hits capacity limits. Distillation—training a small student model on high-quality action trajectories generated by larger teacher models (or frontier APIs)—enables small models to learn structured output conventions that would otherwise require multi-billion parameter capacity.</p>\n<h2>Real Local Runs, Artifacts, and Lessons Learned</h2>\n<p>Local post-training produces concrete empirical evidence. Reviewing actual runs recorded in posttrainllm reveals key lessons:</p>\n<h3>Case Study 1: <code>qwen3-4b-file-ops-distilled</code> (Routed Specialist Win)</h3>\n<ul><li><strong>Objective:</strong> Improve file-operation tool-calling accuracy on a 4B parameter model.</li>\n<li><strong>Result:</strong> Distillation improved file-operations hard gate task completion from 58.0% to 100.0%.</li>\n<li><strong>Regression Check:</strong> Out-of-domain general planning breadth regressed from 59.6% down to 42.3%.</li>\n<li><strong>Decision:</strong> Released as a <code>routed-ship</code> specialist artifact. The model is deployed exclusively behind a file-ops route; it is explicitly rejected as a general planner replacement.</li></ul>\n<h3>Case Study 2: <code>qwen06-sql-hygiene-dpo-v1</code> (Format Prior Limitations)</h3>\n<ul><li><strong>Objective:</strong> Suppress unwanted prose wrappers (e.g., <code>Answer: ...</code>) around SQL SELECT statements on Qwen3-0.6B.</li>\n<li><strong>Result:</strong> Higher-pressure reference-anchored DPO (<code>β=0.3</code>, 200 steps) successfully minimized preference loss (<code>0.6931 → 0.0073</code>) and boosted inner SQL execution from 86.0% to 92.0%. However, greedy generation still retained the prose wrapper (<code>clean-SQL</code> raw rate remained at 0.000).</li>\n<li><strong>Decision:</strong> <code>retry-data</code>. Diagnosis revealed that the base model's strong prose prior could not be erased by rank-4 preference adapters alone when SFT targets were already clean. Format hygiene required generation-time constrained sampling or stronger initial SFT rather than preference updates.</li></ul>\n<h3>Case Study 3: FLAN-T5-Small Autocorrect Pilot (Overcorrection Failure)</h3>\n<ul><li><strong>Objective:</strong> Train an encoder-decoder model (FLAN-T5-small, 60M parameters) for text error correction using hand-rolled PyTorch MPS LoRA.</li>\n<li><strong>Result:</strong> Ordinary sequence loss over 50 steps caused negative error reduction (+0.0625 to -0.8125). The adapter transformed from a subtle error corrector into an aggressive paraphraser (e.g., changing <code>remeber</code> to <code>remind you</code> rather than <code>remember</code>).</li>\n<li><strong>Lesson:</strong> Sequence loss alone on thin datasets fails to enforce minimum-edit distance constraints.</li></ul>\n<h2>Hardware Constraints and Performance Realities</h2>\n<p>When post-training on Apple Silicon, hardware metrics directly govern viability:</p>\n<table><thead><tr><th>Parameter Count</th><th>Quantization / Format</th><th>Peak Training RSS</th><th>Decode Speed (M5 Pro / M3)</th><th>Typical Use Case</th></tr></thead><tbody><tr><td><strong>0.6B</strong></td><td>BF16 / Float32</td><td>~1.1 GB – 1.8 GB</td><td>~120 – 180 tok/s</td><td>Fast local router, bare SQL, single-intent classifiers</td></tr><tr><td><strong>1.7B</strong></td><td>BF16 / Q4_K_M</td><td>~2.5 GB – 4.0 GB</td><td>~60 – 90 tok/s</td><td>Single-turn tool calling, structured JSON extraction</td></tr><tr><td><strong>4.0B</strong></td><td>BF16 / Q4_0</td><td>~5.5 GB – 9.0 GB</td><td>~30 – 50 tok/s</td><td>Multi-turn agentic planning, routed specialists</td></tr></tbody></table>\n<p>Key hardware lessons:</p>\n<ol><li><strong>Low Loss <code>≠</code> High Capability:</strong> Training loss reaching near-zero (e.g., 0.001) often indicates memorization rather than generalization.</li>\n<li><strong>Unified Memory Headroom:</strong> Always reserve at least 20–30% of system RAM for macOS WindowServer and buffer caches to prevent thermal throttling or page swapping.</li></ol>\n<h2>Clear Next Action</h2>\n<p>To run your own local post-training experiment on Apple Silicon:</p>\n<ol><li>Clone the repository and build the native CLI using <code>swift build -c release</code>.</li>\n<li>Prepare a paired dataset following the run schema contract in <code>docs/factory/run-schema.md</code>.</li>\n<li>Freeze your evaluation baseline before initiating training.</li>\n<li>Execute <code>posttrainllm sft</code> with a low-rank adapter config (<code>r=4</code> or <code>r=8</code>).</li>\n<li>Run <code>posttrainllm eval-gate</code> to generate an immutable report card before deciding whether to ship or retry.</li></ol>",
  },
};

export const articleList = Object.values(articleContent);
