## 1. Release Truth And Safety Gate

- [x] 1.1 Add the typed version 0.1.0 (build 1) Mac release record in a pending-notarization state with no public artifact URL.
- [x] 1.2 Implement a pure fail-closed evaluator for required metadata, verification gates, checksum format, HTTPS, and the reviewed host policy.
- [x] 1.3 Add focused tests covering missing fields, false gates, malformed checksums and URLs, unapproved hosts, and a fully eligible release.

## 2. Human-Readable Distribution Surface

- [x] 2.1 Capture preserve-mode browser evidence for the existing site at 390, 768, and 1440 pixels and initialize the required design-review receipt.
- [x] 2.2 Build the canonical `/download` page in the existing Local Research Bench visual system with pending and verified states driven only by evaluated metadata.
- [x] 2.3 Add a restrained, state-aware homepage entry point without changing primary navigation labels or the existing hero hierarchy.
- [x] 2.4 Add the static public release-metadata route and ensure an ineligible record returns no artifact URL.

## 3. Agent Discovery And Drift Protection

- [x] 3.1 Catalog the release metadata as a machine resource and include the canonical Mac download surface in the generated agent indexes.
- [x] 3.2 Extend agent-surface checks to verify pending-state wording, page/resource separation, and the absence of an ineligible artifact URL.

## 4. Validation And Handoff

- [x] 4.1 Run the focused release tests, browser typecheck, site build, agent-surface check, and the smallest existing relevant project checks.
- [x] 4.2 Review the built page at 390, 768, and 1440 pixels, complete critique and audit passes, and resolve every P0/P1 issue.
- [x] 4.3 Update durable shipped-product documentation only after the checks pass, validate the OpenSpec change strictly, and prepare the reviewed branch for merge.
- [ ] 4.4 Obtain owner `keep` or delegated design feedback before declaring the visual work complete; keep production deployment and binary promotion as explicit follow-up actions.
