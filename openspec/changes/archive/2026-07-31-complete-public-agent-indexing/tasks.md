## 1. Shared Route Truth

- [x] 1.1 Add a post-build generator that merges Astro, Blume, and report-card
  HTML routes into one normalized canonical inventory
- [x] 1.2 Generate substantive Markdown counterparts, `llms.txt` indexes, and a
  complete `/api/ai` catalog from the shared inventory
- [x] 1.3 Generate the merged root sitemap and point robots and page metadata at
  it

## 2. Report Card Discovery

- [x] 2.1 Add canonical, social, robots, and structured metadata to the
  deterministic report-card renderer
- [x] 2.2 Regenerate the public report-card cohort and verify deterministic
  publication output

## 3. Validation

- [x] 3.1 Add focused integrity checks for sitemap, catalog, Markdown, and
  resource boundaries
- [x] 3.2 Run the browser build and smallest relevant project checks
- [x] 3.3 Validate the OpenSpec change strictly and record the completed
  site-health skill run
